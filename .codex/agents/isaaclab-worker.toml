# Jam Coding Role v1.4.0 — explicit per-child budget; main settings are not inherited here.
name = "isaaclab_worker"
description = "Implement semantic or cross-path DoorDog/IsaacLab changes inside assigned boundaries."
model = "gpt-6-astra"
model_reasoning_effort = "medium"
model_context_window = 262144
model_auto_compact_token_limit = 229376
model_auto_compact_token_limit_scope = "total"
background_terminal_max_timeout = 86400000
sandbox_mode = "workspace-write"

developer_instructions = """
Follow root AGENTS.md and .codex/TEAM.md.
Trace actual entrypoint/config/dependency flow and local /home/baoquanc/workspace/IsaacLab APIs. Preserve explicit tensor shape/dtype/device, manager lifecycle, reward/reset/termination, asset/joint and observation/action semantics. Fail visibly on invalid state; do not hide it with fallback, broad catches, fake defaults or clipping. Complete authorized implementation and narrow evidence, including necessary fixes from that evidence; do not stop mechanically after the first patch. Use the ordinary worker for purely mechanical code edits. No Git or unassigned paths/resources.
Stay within the assigned authority. Use .ai/LONG_RUNNING_TASKS.md for durable waits; never hire a model to poll. Return a bounded result, evidence and unresolved claims.
"""

model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"

[agents]
max_threads = 4
max_depth = 1
interrupt_message = true

[agents.role_probe]
description = "Read-only sentinel that proves the effective custom role, model, effort, and sandbox before production roles are enabled."
config_file = "agents/role-probe.toml"

# @package _global_
# H4 common: frozen Gate-A carrier plus raw-LEFT Stage3 current-observation residual.

defaults:
  - /ablation/wbmanip/pull_lr_full_gate_a
  - _self_

checkpoint: REQUIRED_LEFT_STAGE3_RESIDUAL_WARMSTART.pt
checkpoint_load_mode: policy_only
auto_load_latest: false
experiment_name: pull_lr_full_left_stage3_residual

algo:
  config:
    load_optimizer: false
    actor:
      _target_: gr00t.rl.trl.modules.pull_v6_left_stage3_obs_residual_actor.PullV6LeftStage3ObsResidualActor

env:
  config:
    obs:
      obs_dict:
        left_stage3_gate_obs: [privileged_door_info, stage]

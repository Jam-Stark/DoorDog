# @package _global_
# Q-stage seam specialist with explicit current hinge velocity (136D actor / 141D critic).

defaults:
  - /ablation/wbmanip/pull_v6_1_Q_onpolicy_d25_frozen_specialist
  - _self_

experiment_name: pull_v6_1_Q_dynamics_obs_onpolicy_frozen_specialist

env:
  config:
    obs:
      obs_dict:
        actor_obs: [
          dof_pos, relative_to_door, dof_vel, actions, projected_gravity, door_dof_pos,
          base_lin_vel, base_ang_vel, hand_force, stage, privileged_door_info, delta_actions,
          gripper_handle_transform, a2_base_command_raw, a2_base_command,
          z_a2_pull_v6_hinge_velocity, z_a2_pull_v6_release_mode
        ]
        critic_obs: [
          dof_pos, relative_to_door, dof_vel, actions, projected_gravity, door_dof_pos,
          base_lin_vel, base_ang_vel, hand_force, stage, privileged_door_info, delta_actions,
          gripper_handle_transform, a2_base_command_raw, transition, complete, time_in_stage,
          actual_time_in_stage, total_time, a2_base_command,
          z_a2_pull_v6_hinge_velocity, z_a2_pull_v6_release_mode
        ]

# @package _global_
# P2-r6ao gives the proven Stage-5 traversal enough time to reach whole-body clear.

defaults:
  - /ablation/wbmanip/pull_v6_F0_r6an
  - _self_

checkpoint: logs_rl/a2_piper_pull_v6/pull_v6_F0_r6an_seed3/model_step_000025.pt
checkpoint_load_mode: policy_only
auto_load_latest: false
experiment_name: pull_v6_F0_r6ao

env:
  config:
    max_stage_time: [250, 100, 100, 100, 250, 500]
    max_episode_length_s: 30

# DONE

- 2026-08-05 03:40 HKT - 读取 plan R3 / manifest R3 / change log，确认 admission chain、GPU 边界与 source facts；建立本 memory entry。
- 2026-08-05 05:30 HKT - 实现 §5A hinge damping randomization plumbing：`door.py` 新增 `hinge_drive_damping_range` / `rand_hinge_drive_damping` 与统一的 `_resolve_hinge_drive_scalar` 写入路径、metadata 还原；`generate_door_assets.py` 导出 per-asset damping；`scenario_cfg/isaacsim.py` 新增 v22 deterministic manifest selector 与 H0-H4 bucket mixture selector（bucket 由 runtime 值判定，不得由 scenario name 推断）。
- 2026-08-05 06:00 HKT - 新建 `gr00t/rl/envs/door/a2_v22_evidence.py`（posture need / hysteresis / reward / clearance / bucket / free-return 分类的纯函数），并在 `door_open_a2_base.py` 接入 v22 plan id、状态缓冲、per-step 更新、reset、六个 reward term、trace 字段与 log telemetry；`a2_base.py` 新增受 `a2_v22_posture_intervention_probe` 保护的 posture intervention 通路。
- 2026-08-05 07:08 HKT - P0-D 完成（door-only IsaacLab 探针，GPU0）：runtime USD drive 属性与请求值逐条一致，证明 damping randomization 通路生效；测得并发布 §2.3 单位约定——USD angular drive stiffness/damping 为 per-DEGREE，IsaacLab 交给 implicit actuator 时按 180/π ≈ 57.2958 换算为 per-RADIAN，maxForce 原值即 N·m effort limit。free-return 探针对 damping 单调（`damping_increases_half_time=True`）。
- 2026-08-05 07:08 HKT - §6.2 fixed external torque ladder（5/10/15/20 N·m，两个世界符号都测）在本 asset 上无分辨力，如实标记 `FIXED_TORQUE_PROBE_INCONCLUSIVE_BELOW_RESOLUTION`；`V22_HINGE_RANGE_FREEZE.json` 只冻结实测到的 H0/CORE、H1/HIGH_DAMPING、H2/FAST_REBOUND，H3/H4 记为 unrealized，未使用未授权的 Appendix A 数值。
- 2026-08-05 08:50 HKT - P0-A 完成：实测 pitch response −0.1743 rad vs crosstalk −0.0185，roll response −0.1361 vs crosstalk +0.0017，axes 可分；command 布局 `[x,y,yaw,pitch,roll]`（idx3/idx4）与 achieved `rpy`（idx0=roll, idx1=pitch）顺序相反且 achieved ≈ −command，均由实测确认而非读源码。
- 2026-08-05 08:50 HKT - P0-C 完成（15 个 posture grid cell + legacy control，16 env，GPU0/GPU1）：有效 live-grasp 帧随 commanded pitch/roll 单调增加，负 pitch 端几乎无法成功握持，与 v20 P2 pitch-clamp 塌陷一致。nominal posture 表在所有 13 个可测高度上均为 0（neutral 始终在 best 的 5% 内或无 bounded cell 同时满足 capacity+margin）；wrench 下尾阈值 209.92 N，tracking p90 2.2553 rad；3 个高度因 neutral cell 覆盖不足被显式排除而非插补。
- 2026-08-05 09:00 HKT - P0-POSTURE-BASELINE 完成：12/16 contributing episodes 达标但 `ordinary_need_negative` 仅 79 帧（<1000），判定 `REPORT_ONLY_INSUFFICIENT_DENOMINATOR` + `POSTURE_NEED_OVERACTIVE_OR_VACUOUS`。实测根因：arm hard-limit joint margin 在有效握持期间 p10 ≈ 0（p50 ≈ −2e-5），§7.3 的 workspace 判据几乎恒真。保留预注册 0.15 阈值（实测下尾退化，改用它会让 workspace_need 永不触发），并按 §7.6.4/§7.6.5 签发 `V22_GATE_WAIVER_POSTURE_GATES_REPORT_ONLY.json`。
- 2026-08-05 09:05 HKT - 64 env / 10 iteration smoke test 通过（rc=0，checkpoint 与 metrics 正常）。按 §7.5 实测 income share 做一次 Window-A 标定：`penalty_a2_v22_posture_saturation` −4.0 → −16.0（0.9% → 4.2%，落入 2-8% band）；`excess_posture` 与 `posture_feasibility` 低于 guidance band 的原因如实记录在 `V22_REWARD_CALIBRATION.json`，未强行放大 scale。
- 2026-08-05 09:11 HKT - Wave 1 formal 训练在独立 tmux session `base_v22_formal_v3` 启动：G1（GPU0, seed0）与 G2（GPU1, seed1），各 4096 env / 2500 batch / save250，warm start `policy_only`。09:35 确认存活：G1 batch 32、G2 batch 31，metrics 全部有限，~24 s/iteration，ETA ≈ 17 h，与 §18.3 实测预算一致；只占用 physical GPU 0/1。

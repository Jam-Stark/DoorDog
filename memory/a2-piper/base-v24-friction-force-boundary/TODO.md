# TODO

## Active Ordered Work

1. **P2 — directional capacity / lambda / ladder / E-region certificate.** Compute directional capacity and `lambda`, recalibrate the friction-gated ladder, then freeze the E-region certificate. This includes the previously `NOT_PERFORMED` parameter-range freeze; P2 is admitted by `V24_FRICTION_MODEL_VALID_BEHAVIORAL` but has not executed.
2. **P3 — frozen historical zero-sample scan.** Run the R1 P3 scan only after P2's required freezes. `V24_FRICTION_AXIS_NONDISCRIMINATIVE` is the only Owner decision point; no owner decision is required at the completed D-v2 gate.
3. **After P3 GO — continue R1 Wave 1 and later conditional work.** Proceed with Wave 1+ under R1 only after the P3 result admits continuation. Training, causal, release, and downstream-wave results are not recorded here.

2026-08-17 01:40 HKT - The Owner D-v2 revision supersedes `FINAL_STOP_AT_P1` as the round terminal while retaining `V24_FRICTION_AUTHORITY_INSUFFICIENT` as historical R1 provenance. D-v2 behavioral energy accounting is `V24_FRICTION_MODEL_VALID_BEHAVIORAL`; P2/P3 admission is true.

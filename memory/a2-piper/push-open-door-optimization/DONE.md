# DONE

- 2026-07-16 22:39 HKT - 完成 `v13_A`/`v13_B` training-ready implementation。A 配置覆盖 M2 `800/25` + squeeze min2、M3 base unlock、M4 v12_C step3000 policy-only warm-start、M5 grasp-gated unlatch/hold-and-drive `3/8` 与旧 handle reward归零、M6 grasp-conditioned stage3→4、M9 telemetry；B 保持 v12_C `80/3`、base locked、handle6、stability1、velocity iterations1，只启用 control-step gate/warm-start/M9。用户最终确认4-rank时两组均使用 `1024 env/rank`，每组 global rollout batch4096，A cap3000、B cap1500、save250。
- 2026-07-16 22:39 HKT - 完成 v13_A/B targeted validation 与 runtime smoke：78 tests、Python compile、Hydra compose、diff/reward/config/IsaacLab API static review PASS；M9 新增连续 checkpoint exact-zero WARNING CLI。A GPU0–3/port29513、B GPU4–7/port29514 并发运行 `64 env/rank × 50 batches`，两组都完成 iteration50 并写出 `model_step_000050.pt`/`last.pt`，step50 checkpoint CPU load PASS，runtime telemetry 无 NaN/shape/config failure。Trainer 完成后的 Kit shutdown hang 由 Ctrl-C 释放，故 training/checkpoint PASS，但 launcher clean exit 为未验证；formal long training 尚未启动。
- 2026-07-16 21:09 HKT - 完成 `v13_pre` M1 implementation：A2 grasp gate 新增 required `a2_grasp_gate_mode=control_streak|physics_history` 与 `a2_grasp_streak_control_steps=5`；control mode 每个 control step 只在 `_pre_compute_observations_callback` 的 full callback 读取 contact sensor latest physics frame `[:,0]` 并更新一次 stage2 squeeze/stage3-4 both-contact streak，reset/stage switch 清零，completion/stability getter 只读不递增。新增 stage-scoped p50/p90、`streak_ge_K_frac` 与 trace fields；legacy `physics_history` 保持显式 ablation 路径。72 个 targeted no-sim tests、Python compile、YAML parse、Hydra compose、diff check PASS；Black/Ruff 未安装，未声明其 PASS。
- 2026-07-16 21:09 HKT - 完成 M10 matched runtime：v12_C step3000、seed0、16 env、per-env first episode，仅相对原 v12_C eval 增加 `control_streak/K=5`；runtime config 复核保持 Kp/Kd `80/3`、stage3 base locked、`push_door_handle=6`、stage3/4 stability scale `1`、PhysX velocity iterations `1`。最终 artifact `logs_eval/base_v13/base_v13_pre_gatepatch_C3000_matched_scalar_trace_16env_seed0_20260716_r2/` 为 16/16 stage3 entry、0/16 stage4、0/16 goal；首过 stage2 control-step min/median/max `16/20.5/27`，stage3 pooled bilateral `99.815%`、control-streak stability `98.692%`，hinge max mean `.000918rad`。因此根因 A runtime PASS，根因 B/C 与 v13_A behavior 尚未验证。
- 2026-07-16 00:29 HKT - 完成 `base_v12` A/B/C/D current-source v10_A-style scratch factorial configuration/static evidence sync：A `80/3+.5`、B `160/6+.5`、C `80/3+1.0`、D `160/6+1.0`；四组均 checkpoint null、planned 2 ranks、literal `num_envs=4096` per rank、global cap3000/save250。strict YAML/resolved compose/factorial/mapping、CODE_QUALITY、IsaacLab static/no-sim semantics 与 NO_SIM_QA PASS。`base_v12` 不是 v9 reproduction（v9 是 v8_A policy-only warm-start，historical source byte equivalence 未证实）；未启动 training/eval/IsaacSim/runtime，故没有 behavior、resource feasibility、checkpoint I/O 或 winner evidence。
- 2026-07-15 21:18 HKT - 记录 future qualitative render 默认资源口径：保持 matched checkpoint/seed/episode semantics，但从 matched eval env 中随机选2个 env，仅渲染这2个；每个 env 生成 default、handle-side、handle-top 3个 camera video，总计 `2 × 3 = 6` 个视频。除非用户明确要求，不再默认16-env全量 render；该2-env subsample 只做定性解释，不能替代16-env scalar/trace primary evidence。
- 2026-07-15 20:56 HKT - 完成 `base_v11_repair_r1` A/B training/eval evidence sync：两组均从 staged v11_C step1550 policy-only 启动，seed0、literal `num_envs=4096`、500 batches/save125；唯一语义 A/B diff 为 `push_door_handle: 6→0`，hinge 均为6。step125/250/375/500 的八个 seed0、16-env、per-env first-episode eval 全为 `0/16 goal`、`0/16 stage4`、`stage_overtime`。
- 2026-07-15 20:56 HKT - 记录 repair_r1 evidence boundary：A500 mean max hinge `.002081rad`（`.25rad` threshold 的 `.832%`）与 j8/base/workspace/doorframe regression co-occur；B 保留 bilateral/stability stable hold 与 j8 guardrail，但 hinge flat。没有 promotable checkpoint、statistical winner 或 causal root cause；不得仅为更晚 step 延长任一 arm。当时提出的 approval-gated、stage3-exposed `base_v11_repair_r2` single-variable intervention 已由 current `base_v12` scratch factorial supersede，未执行。
- 2026-07-15 20:56 HKT - 完成 `logs_eval` historical layout migration 与 future co-location contract：首批129个 version-named dirs grouped 至 `base_v0..base_v11`；48个旧 top-level Hydra dirs whole-tree 迁移，其中36个 exact-paired 到 `.hydra_provenance/<old-top-dir>`、12个 versioned/unpaired 到 `base_vN/_unpaired_hydra/<old-top-dir>`。`_eval_inputs`/`replay_v2` 仍 top-level、总计14项、无 symlink/collision；未收集 content hash，故只声明 layout/static evidence。
- 2026-07-15 20:56 HKT - `base_eval.yaml` 已令 `eval_log_dir` alias `eval_output_dir`，render default 为 `${eval_output_dir}/renderings`；future base-specific A2 eval 使用 `logs_eval/base_vN/<eval-run>/`，folder 直接承载 `.hydra`、logs、metrics/traces/diagnostics 与可选 renderings，`eval_name` 仅 leaf。config tests `3 passed` 与 resolved compose PASS；实际新 eval runtime 未验证。
- 2026-07-14 22:02 HKT - 完成 `base_v11` incomplete-budget artifact 核验：A/B 的 usable state 为 `500/1000/last=1150`，C 为 `1250/1500/last=1550`；九个 full-state checkpoint payload 均 ZIP integrity 与 CPU load PASS。三组都未达到计划 global step2000，训练停止原因与 step2000 行为未证实，不能作为 full-budget verdict。
- 2026-07-14 22:02 HKT - 完成 v11 matched module scalar/trace eval：seed0、16 env、per-env first episode 的九个 state 全为 `0/16 goal`、`0/16 stage4 entry`、`stage_overtime`；A/B 均 max-stage2，C 均 max-stage3。A/B aligned hinge 完全相同且无 stage3 exposure，故 hinge12 未测试 H+；C step1550 mean max hinge 约 `.001110rad`，bilateral `100%`、stability `99.159%`、close `100%`、over-force `0%`、j7/j8 proximity `0%`，stable hold 不等于 door progress。
- 2026-07-14 22:02 HKT - 记录 v11 evidence boundary：C 的 realized hold bundle 约 `.1813` 对 hinge reward 约 `.00007` 是 reward-magnitude imbalance observation，hold-dominant causality 未证实；无新增 render/定性事件，scalar/trace 为 primary。关键 source mtime 晚于 v10_D launch、早于 v11，A 是 exact saved-config 而非 exact-source control，observation-order diff 仅为 plausible confound。当时的 approval-gated stage3-exposed H+ resume 方向未启动，已由 repair_r1 执行证据 supersede。
- 2026-07-14 14:41 HKT - 已核对 `base_v11` 三组 live launch 与 process environment：A/B 分别使用 GPU `2,3` / `4,5`，都是 scratch global target `num_total_batches=2000` 与 `save_frequency=500`，唯一变量为 `push_door_hinge: 6→12`；C 使用 GPU `6,7`，从 v10_D step1000 做 full-state resume，global target `num_total_batches=2000`、`save_frequency=250`。三组都为 2 processes、literal `num_envs=4096`、seed0、online W&B；GPU 0/1 未分配给这三组。当前只证实 launch/process contract，checkpoint 与训练结果尚未验收。
- 2026-07-14 14:41 HKT - 记录 full-state resume 的 durable gotcha：`algo.trl.num_total_batches` 是全局 iteration 上限，不是 remaining iterations。Checkpoint 恢复 `state.global_step`，trainer 以 `num_total_batches` 设置 `state.max_steps`，default flow callback 在 `global_step >= max_steps` 时终止。因此 step1000→step2000 resume 必须传 `num_total_batches=2000`；传 `1000` 会立即命中终止条件。该 runtime 经验 supersede “传 remaining count=1000”的错误解读。
- 2026-07-14 01:11 HKT - 完成 `base_v10_A/B/C/D` 四个独立 scratch run 的 provenance 核验：四个 `model_step_001000.pt` 都存在且 ZIP integrity PASS；saved config/overrides 共同证明 `checkpoint=null`、`auto_load_latest=false`、literal `num_envs=4096`、2 ranks、seed0、1000 batches 与 online W&B。无效且已清理的 `20260713_173141` setsid launcher 不属于有效实验。
- 2026-07-14 01:11 HKT - 完成 matched scalar/trace runtime：四组均用各自 step1000、seed0、16 env、each-env first episode；全部 `0/16 goal`、`stage_overtime`、无 stage4。A/C 最高只到 stage2，B/D 全部到 stage3；因此 A/B、B/C、C/D 的因果解释受 learned route/stage exposure 限制，单 seed 不产生 statistical winner。
- 2026-07-14 01:11 HKT - 记录 D bounded hold reference：stage3/4 bilateral contact `100%`、contact stability `99.147%`、j7/j8 open-limit proximity `0%/0%`，但 hinge max mean 仅 `.001073rad`，未推门、不是 task winner。C 为 open/no-contact basin；其 workspace signal 是 j3 dominant/argmin bottleneck，不是 only-j3 root cause。
- 2026-07-14 01:11 HKT - D matched render 产出 48 个有效 MP4，定性支持 stationary bilateral hold、无可见持续 door rotation；render numeric exit code unverified。strict no-trace artifact QA 为 FAIL：diagnostic flag=false 仍有 base trace output；targeted source diagnosis 证明该 unconditional base trace 在 physics/reward 后写出，且不改 policy action，故不得把整条 render QA 伪报为 PASS。

- 2026-07-13 17:42 HKT - 用户提供历史 verified training command，纠正 v10 resource/launch contract：每组 2 GPU / `--num_processes 2` 时仍传 `num_envs=4096`，并使用 `WANDB_MODE=online`、reward penalty curriculum disabled/fixed scale 1.0、stage2 contact threshold `1.0`、PhysX velocity iterations `1`。先前 `2048 env/rank` 说法不适用于本轮命令，已 superseded。
- 2026-07-13 17:41 HKT - 用户纠正 `base_v10` initialization contract：新版本表示四组各自 random-init long training，而不是继续训练或 warm-start 旧 actor。正式 A/B/C/D 统一 `checkpoint=null`、`auto_load_latest=false`，actor/critic/optimizer/scheduler/global step 全部 fresh；17:36 HKT 的 `base_v9_B policy_only` 设想已 superseded，未形成有效 run。
- 2026-07-13 17:36 HKT - 清理 `base_v10` launcher-only failure：`setsid` wrapper 立即显示 `Done`，但实际 4 个 Accelerate parent 与 8 个 rank orphaned，且 B/C/D 出现 Vulkan/GPU Foundation initialization failure；已精准终止 12 个进程，删除四个 `20260713_173141` partial run、四份 `/tmp` launcher logs 与 marker。没有 step1000 checkpoint，该事件不计入 experiment evidence。后续固定使用四个独立 foreground terminal、distinct ports、约 10 秒 stagger，并在各组 checkpoint 写完后分别 `Ctrl-C`。
- 2026-07-13 16:37 HKT - 新建 `push-open-door-optimization` entry，从 `base_v9` 起接管 A2+Piper full-stage 推门/开门 RL optimization 的 current state 与 TODO；`reward-implementation-goal` 和 `stage0-2-grasp-terminal` 保留各自历史事实，不再拥有 full-stage `base_v9+` active experiment。
- 2026-07-13 16:37 HKT - 归档 `base_v9_A/B/C/D` formal/matched 结果：四组均训练到 step1000 / `262,144,000` timesteps、training goal 0，matched first episodes 全部 `0/16 goal` / `stage_overtime`。A/B 只在 single-seed bounded ranking 下强于 C/D；A provisional hinge lead、B lower rebound，四组都没有 hold/task success。
- 2026-07-13 16:37 HKT - 归档停止前的核心 finding：close command 约 `99%` 但 body8 单侧 dominance/contact stability 近零；j8 在 close target 下被顶到 `-0.035`，Kp80 的最大静态 P response 约 `2.8N/finger`，所以 effort cap 不是唯一解释；higher-gain clamp 没有保留 step40 contact；`friction_override=null` 不等于 zero friction。
- 2026-07-13 16:37 HKT - 归档 matched-clean scientific stop：`0/8 READY`、`8/8 MATCHED_CLEAN_RETREAT_JOINT_LIMIT` 只证明 controller-local safety abort，不建立 physical reachability failure；用户决定停止 `base_v9` oracle/O±/matched-clean 扩展，下一阶段回到 separately-approved `base_v10` RL optimization/retraining plan。
- 2026-07-13 16:37 HKT - 新增 human-facing 总报告 [`scriptsFORhuman/a2_piper_push_open_door_optimization_base_v0_to_v9_20260713.md`](../../../scriptsFORhuman/a2_piper_push_open_door_optimization_base_v0_to_v9_20260713.md)，集中保存 `replay_v2`、base_v0→v9 因果时间线、formal metrics、诊断边界、artifact map，以及从 saved Hydra config 重建的 train/eval/render 命令经验。

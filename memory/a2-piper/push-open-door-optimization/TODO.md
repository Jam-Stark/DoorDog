# TODO

- 2026-07-14 01:11 HKT - 等待用户单独批准后才执行 `base_v11` 最小 scratch pair：`base_v11_A_D_control`=exact saved D，显式保持 `push_door_hinge=6`；`base_v11_B_D_hinge12`=相对 D 的唯一变量 `push_door_hinge: 6→12`。共同使用 v10 fresh/resource contract：`checkpoint=null`、`auto_load_latest=false`、seed0、1000 batches、`--num_processes 2`、命令字面值 `num_envs=4096`、threshold `.25`、`WANDB_MODE=online`、fixed penalty scale、velocity iterations `1`；独立 foreground terminal、GPU pair、port 与约 10 秒 stagger，checkpoint 写完后由用户 `Ctrl-C`。可选 `base_v11_C_D_stability05` 仅在另行批准后加入：relative D、non-cumulative `a2_stage3_stage4_contact_stability: 4→0.5`，不得与 H+ 累加。当前没有训练开始；不恢复 `base_v9` oracle/O±/matched-clean diagnostics。

---
name: stage0-2-grasp-terminal
scope: quickTEST branch stage0-2-only Teacher PPO experiment where stage2 grasp completion is terminal success
status: active
last_updated: 2026-06-25 17:30 HKT
owned_paths:
  - memory/a2-piper/MEMORY.md
  - memory/a2-piper/stage0-2-grasp-terminal/description.md
  - memory/a2-piper/stage0-2-grasp-terminal/TODO.md
  - memory/a2-piper/stage0-2-grasp-terminal/DONE.md
read_when:
  - 开始 quickTEST 分支的 stage0-2-only training config、stage truncation、grasp terminal success 或相关 train smoke 前
  - 需要确认 stage2 grasp completion 是否应作为 terminal success，以及是否可以 resume full 6-stage checkpoint 时
---

# Stage0-2 Grasp Terminal

## Purpose

记录 `quickTEST` 分支的独立实验目标：先训练/验证 A2_Piper Teacher PPO 的 `stage0 -> stage1 -> stage2` 流程，只要求走到并成功握住 door handle；暂时屏蔽 `stage3` 及其后的 open/swing/through 任务。该 entry 不替代 full door-opening 主线，只服务于快速验证 walk、pregrasp、grasp 三段是否能学起来。

## Current Decision

- 2026-06-22 20:29 HKT - 用户确认从 `A2_Piper` 切出新分支 `quickTEST`，用于 stage0-2 grasp-terminal quick test。
- 2026-06-22 20:42 HKT - 已新增独立 config `gr00t/rl/config/exp/wbmanip/door_open_a2_base_stage0_2_grasp_terminal_lstm.yaml`，入口为 `+exp=wbmanip/door_open_a2_base_stage0_2_grasp_terminal_lstm`；默认 `door_open_a2_base_lstm.yaml` 未改动。
- 2026-06-22 21:23 HKT - `PolicyAndValueWrapper` 已改为通过 `object.__setattr__(..., "_a2_base_model", a2_base_model)` 持有 frozen A2_Base TorchScript model，并用 property 暴露访问；该 model 不再注册为 child `nn.Module`，避免 HuggingFace Trainer optimizer parameter scanning 进入 TorchScript `ParameterDict.contains()` crash。
- 2026-06-22 21:31 HKT - `piper_gripper_handle_frame_transformer` 已切换为 A2-local `OrderedTargetFrameTransformer`，保留 `handle -> pregrasp` target order；bounded smoke 已越过旧 target-order fail-fast，当前下一 blocker 是 `_homie_history_length` missing init。
- 2026-06-22 21:35 HKT - A2_Base init path 已显式初始化 `_homie_history_length`，bounded smoke 已越过 `a_history_homie` reset observation；当前下一 blocker 是 `ResetFromDataset` 的 `reset_count` missing init。
- 2026-06-22 21:39 HKT - stage0-2 quick config 已显式关闭 G1 `ResetFromDataset` motion reset，bounded smoke 已推进到 PPO recurrent model forward；当前下一 blocker 是 A2_Base frozen policy injection 的 recurrent minibatch shape mismatch。
- 2026-06-22 22:03 HKT - PPO recurrent A2_Base injection shape mismatch 已按最小边界修复：只在 A2_Base injection 侧将 padded `a2_base_obs` unsplit 回 env-major 后与 `high_level_actions` 对齐，不改 PPO loss tensors 的 env-major contract。
- 2026-06-24 17:36 HKT - A2 terminal orientation diagnostics 已完成 static review，可进入 runtime orientation diagnosis；本次只扩展 `episode_terminal_diagnostics` 字段，不改变 reward、stage transition、complete predicate、reset、camera、rendering timing 或 trainer action logic。
- 2026-06-24 17:41 HKT - A2 terminal orientation diagnostics runtime eval 已完成；结果确认仍是同一类 false-success mode：terminal complete 在 `stage_buf=1` 触发，gripper 仍 open，且 angled open-gripper geometry 可先撞出 two-sided contact/squeeze。
- 2026-06-24 18:17 HKT - A2 Grasp Frame + Contact History Complete Fix 已完成 implementation/static review，并做一处 reviewer 最小修正：A2 complete predicate 除 `stage_buf == STAGE_GRASP` 外，还要求 `actual_time_in_stage_buf >= stage2_grasp_contact_history_length - 1`，确保最近 H 个 contact history samples 都来自 stage2；本次未加入 gripper close/aperture condition，也未改 reward/reset/camera/trainer action logic。
- 2026-06-24 18:26 HKT - main-agent 已完成 A2 Grasp Frame + Contact History Complete Fix runtime validation：旧 false-success checkpoint 在新 target frame + last-stage/history gate 下不再 stage1 complete，`goal_reached=false` 且 `max_stage_reached=0`；该旧 checkpoint 不应 resume，后续必须重新训练。Small PPO smoke 已跑到 `Learning iteration 2`，无 target frame/contact history crash。
- 2026-06-24 21:52 HKT - 新训练 checkpoint `stage2_finished_orientation-20260624_183146/model_step_001000.pt` 已完成 2-env 720p eval；两个 env 均进入 `stage_buf=2` 并在 stage2 overtime，未触发 complete。terminal diagnostics 显示 gripper/target orientation 与 handle distance 已明显改善，但 terminal handle contact force 仍为 0，gripper DOF 基本仍在 open side。
- 2026-06-24 22:11 HKT - A2 stage0-2 eval-only per-step trace diagnostics 已完成 implement reviewer static review，并做一处 reviewer 最小修正：A2 eval trace hook 若未先 init trace buffer 会 fail-fast，而不是 silent skip。新增 `stage2_step_trace.json` 只服务 A2 eval diagnostics，记录 `STAGE_GRASP` 每步的 `gripper_primitive_raw`、`arm_j7/j8`、contact force、squeeze、handle/pregrasp distance 等 terminal-diagnostics semantics；本轮不改 reward、stage transition、complete predicate、reset、camera 或 render timing。
- 2026-06-24 22:19 HKT - `stage2_step_trace.json` runtime eval 已完成；用户观察到的 env0001 grasp-target 附近 gripper open/close 抖动成立，但 trace 显示 handle contact force 与 squeeze 全程为 0，因此当前 ckpt1000 更像是接近 handle 后 gripper primitive 在 0 附近抖动且未形成物理接触，而不是 close grasp 被 handle 挡住后的稳定夹持。
- 2026-06-24 22:45 HKT - A2 Stage2 Close Shaping Rewards 已完成 implement reviewer static review，并做最小修正：`a2_stage2_close_command` 显式使用 `clamp((-primitive - 0.2) / 0.8, 0, 1)`；`a2_stage2_close_progress` 使用 open target 到当前 gripper DOF 的 abs progress / abs open-close span，先 mean two DOFs，再 `clamp(mean_progress / 0.6, 0, 1)`。shared/full 与 quick config 均启用 scale `1.0/0.5` 并加入 `reward_penalty_reward_names`；本轮不改 complete predicate、contact history gate、stage transition、reset、camera、render timing 或 action semantics。
- 2026-06-24 22:49 HKT - A2 Stage2 Close Shaping Rewards bounded PPO smoke 已完成：quick exp 跑到 `Learning iteration 1`，reward functions 正常注册并打印 `rew_a2_stage2_close_command` / `rew_a2_stage2_close_progress`，无 reward shape 或 rollout crash；tiny stage0-only smoke 中两项 reward 为 0 属 expected。
- 2026-06-24 22:54 HKT - 新增 `penalty_base_roll_pitch_l2: -2.0`，只在 `STAGE_WALK_TO_DOOR` 与 `STAGE_PREGRASP` 生效，直接约束 actual base roll/pitch `self.rpy[:, 0:2]` 的 L2，避免 stage0/1 为了接近 door/handle 学出明显倾斜；本轮不改 reset、termination、stage transition、gripper reward 或 reward penalty curriculum routing。
- 2026-06-25 10:30 HKT - 最新训练 checkpoint `stage2_finished_grasp-20260624_225622/model_step_003000.pt` 720p eval + `stage2_step_trace.json` 诊断：两个 env 均在 stage2 overtime，trace 显示 stage2 全程 `gripper_primitive_raw ∈ [-1.2, -0.93]`（close），`arm_j7/j8_pos ≈ [0.005, -0.000001]`（接近 close_target），handle_distance 最小 0.0015m（gate 内），但 contact force 全程为 0——闭合 gripper 撞向 handle 但未夹住。
- 2026-06-25 10:40 HKT - 修复 `_reward_pregrasp_gripper_dof_pos_l1` stage-aware target：原实现 stage0/1 均用 `_a2_gripper_close_target`，导致 stage0/1 全程 reward gripper 闭合，policy 进入 stage2 时 gripper 已闭合，gate 内 `a2_stage2_close_*` 无法形成"张开→闭合"真实抓取。修复后 stage0（STAGE_WALK_TO_DOOR）track `close_target`（行走时收起），stage1（STAGE_PREGRASP）track `open_target`（准备抓取），span 统一用 `open_target - close_target`。`py_compile` 通过；未改 stage2 close rewards、complete predicate、contact history gate、stage transition、reset、camera、render timing 或 action semantics。
- 2026-06-25 17:30 HKT - `stage2_finished_grasp_OPEN-20260625_105101/last.pt` eval trace 诊断：stage1→stage2 gripper 确实张开了（stage0/1 修复生效），但 stage2 gate 外（handle_dist>0.03）gripper 提前闭合——env0 在 dist=0.062 时 prim 从 0.349 转 -0.001，gate 首次触发（dist=0.0255）时 prim 已为 -0.927；env1 同样在 gate 外提前闭合。根因：gate 阈值 0.03m（3cm）过大，close reward 在 gripper 未真正到位时触发；且 stage2 gate 外无 reward 引导保持张开。
- 2026-06-25 17:30 HKT - 收紧 `_get_a2_stage2_close_reward_gate` handle_distance 阈值从 `< 0.03` 改为 `< 0.015`（1.5cm），防止 close reward 过早触发；未改 close rewards 公式、complete predicate、contact history gate、stage transition、reset、camera、render timing 或 action semantics。
- 2026-06-25 20:30 HKT - **quickTEST 已 fast-forward merge 回 A2_Piper 主线**。A 类通用 bugfix 与 B 类通用增强已全部保留在主线；C 类 stage0-2 专属文件（本 config + 本 memory entry）已从主线删除，但 quickTEST branch 仍保留。主线 A2_Piper 新增 `memory/a2-piper/quicktest-merge/` entry 记录完整合并清单与边界。**后续若有新的 quickTEST 更新需要 merge 回主线，可忽略本次已完成的 merge（quickTEST HEAD `34e06b7` 及之前的 commit 均已合并），只需 merge `34e06b7` 之后的新 commit。**
- Stage mapping 保持 Doorman/A2 当前语义：`stage0 = STAGE_WALK_TO_DOOR`，`stage1 = STAGE_PREGRASP`，`stage2 = STAGE_GRASP`。
- 在本 quick test 中，`_stage_2_to_complete_condition()` 表示 terminal success；也就是 A2 branch 的 handle-specific two-sided gripper contact、source local `+Y` squeeze threshold 与 opposite-sign squeeze check 成立后，视为任务完成。
- 默认 full task 仍是 6-stage：`STAGE_OPEN`、`STAGE_SWING`、`STAGE_THROUGH` 不应从主线删除；本实验应通过独立 config 或 branch-local override 实现，不污染默认 `door_open_a2_base_lstm` 语义。

## Source Facts

- 当前 `StagedTaskBase` 通过 `len(env.config.max_stage_time)` 决定 `num_stages`，并把最后一个 stage 的 `_stage_{last}_to_complete_condition()` 注册为 task complete condition。
- 当前默认 A2 env config 是 6-stage：`max_stage_time`、`stage_reward_scale` 与 `staged_reset_ratios` 都是 6 项。
- 当前默认代码中 `_stage_2_to_complete_condition()` 已实现 A2 grasp completion；但在 6-stage config 下，`_stage_2_to_3_advance_condition()` 使用 `completion | door_open_bypass` 进入 stage3，不是 episode terminal。
- `stage` observation dim 来自 `len(env.config.max_stage_time)`。把任务截成 3-stage 会改变 actor/critic input dim，因此不能直接 resume 6-stage checkpoint。
- 2026-06-22 20:42 HKT static validation resolved `obs_dims.stage: 3`，并确认 stage3+ reward scales 在 quick test config 中为 `0.0`。
- 2026-06-22 21:23 HKT reviewer validation: `py_compile` passed；lightweight probe confirmed A2_Base TorchScript is absent from wrapper `named_children()` / `named_modules()` / `named_parameters()` while property access still works；bounded smoke reached `Using frozen A2_Base policy for low-level leg actions` and `===training policy===`，说明旧 optimizer `ParameterDict contains()` blocker 已移除。
- 2026-06-22 21:31 HKT source fact: IsaacLab `FrameTransformer` 对 duplicate target body 的 frame names 使用 `set`，会让同一个 `grasp_target` rigid body 上的 `handle` / `pregrasp` target order 不可靠；A2-local `OrderedTargetFrameTransformer` 只用于该 gripper-handle sensor，并让 `_get_a2_gripper_handle_frame_transformer()` 保持 exact `['handle', 'pregrasp']` fail-fast contract。
- 2026-06-22 21:35 HKT source fact: `A2Base.__init__` 在 `a2_base.enabled=True` 时执行 `LeggedRobotBase.__init__()`、`_init_a2_base_action_chain()` 后直接 return，因此 non-A2/Homie branch 中的 `_homie_history_length` 初始化不会执行；但 A2 obs config 仍在 `homie_obs` 使用 `a_history_homie`，所以 A2 init path 也必须显式初始化该 field。
- 2026-06-22 21:37 HKT source fact: A2 explicit `ResetFromDataset` init 会加载 `${HOME}/projects/LAFAN-G1` G1 motion files，并在 mapping dof names 时用 `left_hip_pitch_joint` 匹配 A2 robot dof，触发 fail-fast mismatch；因此 stage0-2 quick test 应显式禁用该 G1 motion reset path，而不是给 A2 做 name fallback。
- 2026-06-22 22:03 HKT source fact: recurrent minibatch 中 `mb_obs_dict` 使用 padded trajectory layout `[num_trajectories, max_traj_len, ...]`，但 `actions/logprobs/values/returns/advantages/padding_mask` 保持 rollout env-major `[num_envs, num_steps, ...]`；`RecurrentActor` / `RecurrentCritic` training path 会对 padded `memory_out` 执行 `unsplit_trajectories(..., original_dones)`，所以 A2_Base frozen policy injection 的正确最小修复是在 `_a2_base_actions()` 中只 unsplit padded `a2_base_obs`。
- 2026-06-22 22:07 HKT validation fact: main-agent 复跑 1-iteration smoke 已推进到 `Learning iteration 1`，actor/critic obs dim 分别为 130/135，stage3+ reward scales 保持 0.0；原 `flat_obs` / `high_level_actions` shape mismatch 未复现。训练主体完成后 IsaacSim shutdown 未自然退出，已手动 Ctrl-C 清理会话。
- 2026-06-22 22:15 HKT runtime diagnosis: tmux 中若卡在 `gpu.foundation.plugin No device could be created`、`Failed to open display`、`GLFW initialization failed`、`GPU Foundation is not initialized`，这是 IsaacSim/Kit 在 Vulkan/display/GPU foundation 初始化阶段失败，早于 env/trainer；优先比较 tmux 内外 `DISPLAY`、`XAUTHORITY`、`DBUS_SESSION_BUS_ADDRESS` 与 `vulkaninfo --summary`，不要当作 stage0-2 trainer blocker。
- 2026-06-22 22:15 HKT refined runtime diagnosis: 当前 host 普通 terminal 也没有 `DISPLAY/XAUTHORITY`，且没有安装 `vulkaninfo` 诊断工具；但系统存在 NVIDIA Vulkan ICD `/usr/share/vulkan/icd.d/nvidia_icd.json` 与 `/dev/nvidia*`。这更像 headless server runtime，需要使用 `headless=True` 并可显式设置 `VK_ICD_FILENAMES=/usr/share/vulkan/icd.d/nvidia_icd.json`，不要强行设置假的 `DISPLAY=:0`。
- 2026-06-22 22:40 HKT diagnosis: 用户长训 log 已完成 `simulation start`，真正失败点是 `OrderedTargetFrameTransformer._initialize_impl()` 的 fail-fast `FrameTransformer target frame name 'handle' is duplicated`。main-agent smoke 未遇到是因为 smoke 覆盖了 `num_envs=1`，而 quick config 默认 `num_envs=4096`；当前 duplicate check 用 `frame_name` 作为全局 key，会把多 env 中同名 `handle` offset 误判为重复。
- 2026-06-22 22:52 HKT source fact: `OrderedTargetFrameTransformer._initialize_impl()` 的 duplicate check 已改为只遍历 config-level `FrameTransformerCfg.target_frames` names，因此不同 env regex 展开的同名 `handle` / `pregrasp` 不再触发 duplicate；true config duplicate 仍在 sensor initialization 前 fail-fast。
- 2026-06-22 22:52 HKT source fact: `body_names_to_frames` 现在用 list append 记录同 body target frames，保留 cfg target order `handle -> pregrasp`；`_get_a2_gripper_handle_frame_transformer()` 仍校验 `target_frame_names == ['handle', 'pregrasp']`，order regression 会继续 fail-fast。
- 2026-06-22 23:53 HKT diagnosis: 当前 training entrypoint 通过 IsaacLab `AppLauncher` 支持 WebRTC livestream env path（`LIVESTREAM` + `PUBLIC_IP`），但不建议直接加到 `accelerate --multi_gpu --num_processes 4` 长训上；每个 rank 都会启动 IsaacSim/Kit app，默认 WebRTC port/client 会冲突且额外渲染会污染训练吞吐。可视化 quick diagnosis 应用单进程、小 `num_envs`、独立 GPU 复现实验，或等 checkpoint 后单独跑 visual/eval。
- 2026-06-22 23:59 HKT diagnosis: 当前 `LIVESTREAM=1` single-process visual run 的 env 已传入，但 `ss` 未见 `49100` listening，`omni.kit.livestream.log` 未出现 2026-06-22 的 `start` event；`omni.kit.extension.log` 只显示 `omni.kit.livestream.webrtc` / `omni.services.livestream.nvcf` extension startup。`zenity Failed to open display` 本身不是 training blocker，但说明默认 full `isaaclab.python.kit`/native GUI dialog path 仍在 headless host 上被触发。后续 visual run 应清理旧 IsaacSim processes，并显式传 `--experience /home/baoquanc/workspace/IsaacLab/apps/isaaclab.python.headless.rendering.kit`。
- 2026-06-23 00:07 HKT diagnosis/fix: training entrypoint 在 `LIVESTREAM>=1` 或 `headless=False` 时会进入 IsaacLab `AppLauncher._hide_stop_button()` optional toolbar UI path；当前 Kit runtime 缺少 `omni.kit.widget.toolbar`，导致 visual training 在 app launch 阶段 `ModuleNotFoundError`。`train_agent_trl.py` 已加入与 preview entrypoint 相同的 exact toolbar guard，只跳过缺失 toolbar 的 `_hide_stop_button/_hide_play_button`，其他 `ModuleNotFoundError` 继续 fail-fast。
- 2026-06-23 00:13 HKT diagnosis: toolbar guard 后 visual run 已进入 sim startup，并加载 `omni.kit.livestream.webrtc` / `omni.services.livestream.nvcf` extension；但 `ss` 仍无 `49100` listening，livestream log 仍无 2026-06-23 `start` event。当前机器同时残留两个 GPU3 visual runs、两个 GPU4 `headless=False` visual runs 和一个 4-rank long training，GPU4 还被旧 visual 与 rank0 同时占用；`zenity Failed to open display` 与 PhysX GPU pipeline fallback 在此状态下不能单独解释，需要先清理 stale visual processes 后再复测单一 WebRTC run。
- 2026-06-23 00:19 HKT decision: 如果目标是完整 Isaac Sim GUI，而不是 headless livestream smoke，当前 WebRTC route 已低效；建议转向 `xpra`/remote desktop 方案，但前提是提供真实可用的 X display。GUI run 应取消 `LIVESTREAM`，使用 `headless=False`、小 `num_envs`、独立 GPU，并通过 `DISPLAY` 连接 xpra session；`xpra` 不能替代 GPU/Vulkan driver，只负责提供/转发 desktop。
- 2026-06-23 13:41 HKT diagnosis: `xpra start :100 --bind-tcp=0.0.0.0:14500 --html=on` 已成功启动，`xpra list` 显示 live session 且 `ss` 显示 `14500` listening；但该 session 后端是 `Xvfb-for-Xpra-:100`，`DISPLAY=:100 glxinfo -B` 显示 `OpenGL renderer string: llvmpipe` / `Accelerated: no`。因此当前 `:100` 只能用于验证 xpra/web client，不是 Isaac Sim full GUI/Vulkan 的合格 GPU-backed display。
- 2026-06-23 19:57 HKT visualization route: 当前主训练 run `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/door_open_a2_base_stage0_2_grasp_terminal_lstm-20260622_233941/` 已产出 `model_step_001000.pt` 到 `model_step_004000.pt`；最新 checkpoint 可用 headless eval + `simulator.config.render_results=true` 生成 observer-camera mp4。由于 `eval_agent_trl.py` 的 base config 很小，checkpoint eval 覆盖训练 config 的 key 需要使用 Hydra `++` 语法，例如 `++headless=True`、`++num_envs=1`、`++simulator.config.render_results=true`；同时必须显式 `++headless=True` 覆盖训练 config 内 `eval_overrides.headless: false`，避免走当前不合格的 llvmpipe xpra display。
- 2026-06-23 20:03 HKT eval smoke result: `model_step_004000.pt` 的 `num_envs=2` headless eval 已完成 env reset、A2_Base low-level action injection、2 episodes rollout 与 render writer creation，生成 `logs_eval/a2_stage0_2_eval_smoke_ckpt4000/renderings/2026-06-23_20-02-29_viewer_{0,1}.mp4`。失败点在 rollout 完成后的 `json.dump(eval_dict)`，`metrics_eval.json` 只写到 `episode_goal_reached` 即因 tensor/numpy value 不可 JSON serialization 中断；这不是 policy/env step blocker。生成的视频约 `0.1s / 5 frames`，其中 viewer_0 首帧近白、viewer_1 只看到远处 door，说明当前 `render_results` camera/timing 还不足以判断 policy 效果。
- 2026-06-23 20:12 HKT eval serialization fix: `ppo_trainer_a2_base_api.py` 已在写 `metrics_eval.json` 前把 eval metrics 显式转换为 JSON-safe data，支持 JSON-native、`torch.Tensor`、`numpy.ndarray`、`numpy.generic`、`dict/list/tuple`，未知类型仍 fail-fast 报 metrics key path；metrics 通过 temp file + `os.replace()` 原子写入，避免失败时留下半截 JSON。最终代码版本复跑 `model_step_004000.pt` 2-episode eval smoke 已完整写出 `logs_eval/a2_stage0_2_eval_smoke_ckpt4000/metrics_eval.json`。
- 2026-06-23 20:33 HKT eval true-episode rendering review: `render_results` 已改为 per-env writer lifecycle，eval loop 在 `reset_all()`/per-env reset 后写 initial frame，在 rollout 中只给 non-terminal env 写 step frame，`_post_physics_step()` 在 `reset_envs_idx()` 前写 terminal frame 并 close/rename mp4，避免同一 writer 跨 episode stitching。`eval_rendering` contract 支持 `env_static` 与 `root_tracking`，缺 key、未知 `camera_mode`、非法 `fps` 或非 bool frame flags 均 fail-fast。
- 2026-06-23 20:33 HKT eval diagnostics review: `metrics_eval.json` summary 现在包含 `episode_lengths`、`episode_rewards`、`episode_goal_reached`、`episode_max_stage_reached`、`episode_terminal_reasons`，terminal reason set 覆盖 `complete/stage_overtime/episode_timeout/low_height/bad_orientation/door_distance/upper_dof_overspeed/unknown_reset`。review validation 通过 `py_compile`、targeted `git diff --check` 与 no-IsaacSim helper smoke；完整 IsaacSim/headless eval runtime smoke 仍建议由 main-agent 复跑确认 mp4 画面质量。
- 2026-06-23 20:44 HKT runtime validation: `model_step_004000.pt` 的 saved training config 早于 `env.config.eval_rendering`，因此 checkpoint eval 需要显式 CLI override `++env.config.eval_rendering.*`；代码仍保持缺 key fail-fast，不做 silent default injection。
- 2026-06-23 20:44 HKT runtime validation: 首次 true-episode eval smoke 暴露一个 recorder lifecycle issue：terminal env 在 env step 内 reset 后，trainer 先按 `dones==0` 写 step frame，会给已完成 quota 的 env 打开下一 episode writer，产生 `reason-rollout_end` 尾巴 mp4。修复后 step frame 写入移动到 completion bookkeeping 之后，并过滤 `eval_num_envs_episodes` 下已完成 env，不改变 termination/reset/reward semantics。
- 2026-06-23 20:44 HKT runtime validation: 最终 `num_envs=2`、`eval_num_envs_episodes=true`、`render_results=true` eval smoke 成功写出两个正式 mp4，无 `.writing.mp4` 残留：`env0000_episode0000_len5_reason-bad_orientation.mp4` 为 6 frames，`env0001_episode0000_len4_reason-bad_orientation.mp4` 为 5 frames；`metrics_eval.json` 可解析，episode lengths 为 `[4, 5]`，terminal reasons 为 `["bad_orientation", "bad_orientation"]`，goal reached 均为 `false`。当前短视频原因是 policy/env 真实 early termination，不再是 recorder truncation。
- 2026-06-23 21:15 HKT reset stabilization review: `door_open_a2_base.py::_reset_root_states()` 的 A2 default path 仅在 `_use_a2_base` 下生效；`target_root_states is not None` 仍委托 `A2Base._reset_root_states()` 保持 parent/fail-fast contract。A2 default reset 先复制 `base_init_state` 并加 `env_origins`，保留 z/default roll-pitch，再覆写 x=`U(-1.5,-0.6)+origin_x`、y=`U(-0.5,0.5)+origin_y`、yaw=`U(-pi/4,pi/4)`，并清零 root vel `[7:13]`。`_reset_dofs()` 的 A2 default path 使用 `default_dof_pos * U(0.8,1.2)` 且 DOF vel 置零；target path 仍委托 parent。Non-A2/G1 reset hunk 未改旧逻辑。
- 2026-06-23 21:15 HKT reset stabilization validation: review 未发现 reset patch 改动 reward/stage transition/termination threshold；同文件中 terminal reason diff 属于既有 eval diagnostics 标记，保持原 `reset_buf` predicate 语义。`py_compile`、targeted `git diff --check` 与 Hydra compose 均通过；`model_step_004000.pt` 的 2-env headless eval 输出到 `logs_eval/a2_stage0_2_eval_reset_stabilized_ckpt4000/`，`metrics_eval.json` 可解析，episode lengths `[452, 452]`，max stages `[2, 2]`，terminal reasons `["stage_overtime", "stage_overtime"]`，goal reached 均为 `false`；两个 mp4 各 453 frames，之前 4/5 step `bad_orientation` 已消失。
- 2026-06-23 21:27 HKT eval camera review: `env.config.eval_rendering.camera_mode` 新增并默认使用 `door_top_down`；该 mode 以 `self.simulator.get_task_root_state("door")[:, :3]` 作为 anchor，`camera_eye` / `camera_lookat` 继续解释为 door root relative offsets。`base_task.yaml` 默认值为 `camera_eye: [0.05, 0.0, 2.0]`、`camera_lookat: [0.0, 0.0, 0.0]`。缺 `eval_rendering` / required key、unknown `camera_mode`、非法 `fps` 或非 bool frame flags 均保持 fail-fast，不注入 fallback default。
- 2026-06-23 21:27 HKT eval camera validation: code review 未发现 door-top-down camera patch 改动 reward、stage transition、reset、termination predicate 或 trainer action logic；指定 `py_compile` 通过，targeted `git diff --check` 通过。完整 IsaacSim runtime eval 留给 main-agent 复跑。
- 2026-06-23 21:50 HKT runtime camera diagnosis: Hydra config 与 `TiledCamera.set_world_poses_from_view()` 调用均已生效，但 mp4 画面仍像旧视角；root cause 是 IsaacLab `TiledCamera` 的 `XformPrimView` 默认只写 Fabric transform，tiled render product 仍消费 USD-authored camera transform。`render_results()` 现在在写 pose 前 fail-fast 检查并打开 `_sync_usd_on_fabric_write=True`，随后 `sim.render()` 与 `eval_camera.update(..., force_recompute=True)` 再读取 RGB。
- 2026-06-23 21:50 HKT runtime camera validation: extreme probe `camera_eye=[-2.5,-2.5,2.2]` / `camera_lookat=[-0.5,0.0,0.45]` 输出到 `logs_eval/a2_stage0_2_eval_camera_extreme_probe_usdsync_ckpt4000/`，画面明显变为 oblique robot+door，确认 USD sync camera write 生效；用户指定的 `[0.05,0,2]` probe 输出到 `logs_eval/a2_stage0_2_eval_door_top_down_usdsync_probe_ckpt4000/`，画面已变为 door-relative top-down，但视野太近，只能看到门/把手边缘和局部机器人。
- 2026-06-23 21:50 HKT eval camera default update: `base_task.yaml` 的 default eval camera 仍使用 `camera_mode: door_top_down` 与 door root anchor，但默认 offsets 改为已验证更可观察的斜俯视：`camera_eye=[-2.5,-2.5,2.2]`、`camera_lookat=[-0.5,0.0,0.45]`。
- 2026-06-23 21:58 HKT runtime camera validation: 已清理 `logs_eval` 下旧 `renderings/`、`previews/` 与 `camera_compare_previews/`，保留历史 `metrics_eval.json`；用 `model_step_004000.pt` 重新跑 2-env complete eval，输出到 `logs_eval/a2_stage0_2_eval_oblique_full_ckpt4000/`。两个 episode 均 `len452_reason-stage_overtime`，mp4 均 453 frames，preview 确认斜俯视可同时看到 robot、door 与 handle 区域。
- 2026-06-23 22:10 HKT eval rendering resolution update: `render_results` 用的 `eval_camera` 原先在 `isaacsim.py` 固定为 `256x256`，不受 ego camera 的 `camera_resolutions` 影响；现在新增 `simulator.config.cameras.eval_camera_resolutions`，默认 `[720,1280]`，并在 `render_results=true` 时 fail-fast 校验为正整数 `[height,width]` 后创建 `TiledCamera`。旧 checkpoint eval 仍需显式 CLI override 该 key。
- 2026-06-23 22:10 HKT runtime camera validation: 已按用户要求清空整个 `logs_eval` 目录内容，然后用 `model_step_004000.pt` 重录 2-env 720p complete eval，输出到 `logs_eval/a2_stage0_2_eval_oblique_720p_ckpt4000/`。两个 mp4 均为 453 frames，frame shape 为 `(720,1280,3)`，episode lengths `[452,452]`，terminal reasons `["stage_overtime","stage_overtime"]`，preview 画面明显比 256x256 更清晰。
- 2026-06-24 16:35 HKT runtime validation: 最新训练 run `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/stage2_finished-20260623_221222/model_step_004000.pt` 已用 door-root anchored 斜俯视 720p eval 重录到 `logs_eval/a2_stage0_2_eval_stage2_finished_720p_ckpt4000/`。两个 mp4 frame shape 均为 `(720,1280,3)`，episode lengths `[83,108]`，episode goal reached `[true,true]`，terminal reasons 为 `["complete+upper_dof_overspeed","complete"]`，episode max stage reached `[1,1]`。
- 2026-06-24 16:40 HKT diagnosis: 最新 720p eval 不能直接解释为“已进入 stage2 并完成 gripper close grasp”。`StagedTaskBase._check_termination()` 当前计算 `is_complete = stage_complete_cond_func() & episode_length>=2`，没有 gate `stage_buf == last_stage`；因此 `_stage_2_to_complete_condition()` 可以在 `stage_buf=1` 时触发 reset-on-complete。metrics 中 `episode_max_stage_reached=[1,1]` 与 terminal reason `complete` 同时出现，说明这次 success 至少存在 final-stage gating 语义漏洞。
- 2026-06-24 16:40 HKT diagnosis: A2 `_stage_2_to_complete_condition()` 当前只检查两侧 gripper body 对 handle 的 contact force、source-local `Y` squeeze magnitude 与 opposite sign，不检查 `arm_j7/arm_j8` 是否接近 `gripper_close_target`。同时 A2 `_reward_grasp_finger_dof_pos_l1()` 在 `_use_a2_base=True` 时返回 0，reward scale 也为 0；stage2 没有显式 gripper close shaping。因此视频中 gripper center 接近 handle target 但 gripper 未闭合，与当前代码语义一致，不能当作真实 grasp close success。
- 2026-06-24 17:19 HKT runtime validation: `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/stage2_finished-20260623_221222/model_step_004000.pt` 已用 A2 Eval Terminal Diagnostics 复跑到 `logs_eval/a2_stage0_2_eval_stage2_finished_terminal_diag_ckpt4000`；`metrics_eval.json` 可用 `python -m json.tool` 解析。两个 720p mp4 分别为 `2026-06-24_17-17-35_env0000_episode0000_len83_reason-complete+upper_dof_overspeed.mp4`（84 frames）和 `2026-06-24_17-17-35_env0001_episode0000_len108_reason-complete.mp4`（109 frames），frame shape 均为 `(720,1280,3)`。
- 2026-06-24 17:19 HKT runtime validation: top-level metrics 为 `completed_episodes=2`、`episode_lengths=[83,108]`、`episode_terminal_reasons=["complete+upper_dof_overspeed","complete"]`、`episode_max_stage_reached=[1,1]`、`episode_goal_reached=[true,true]`，且 `episode_terminal_diagnostics` length 为 2，与 episode arrays 对齐。
- 2026-06-24 17:19 HKT diagnosis: terminal diagnostics 确认两条 success 都发生在 `stage_buf=1`，不是 stage2。env0 `episode_length_buf=83`、`time_in_stage_buf=-168`、`terminal_reasons=complete+upper_dof_overspeed`；env1 `episode_length_buf=108`、`time_in_stage_buf=-143`、`terminal_reasons=complete`。两条 episode 的 `arm_j7_j8_pos=[0.035,-0.035]`，`arm_j7_j8_close_target=[0.0,0.0]`，close error 仍为 `[0.035,-0.035]`，说明 gripper DOFs 处在 open target 而不是 close target；`gripper_primitive_raw` 分别为 `[1.018043041229248]` 与 `[0.40541040897369385]`，均为正向 open command。
- 2026-06-24 17:19 HKT diagnosis: env0 terminal `target_pos_source_handle_distance=0.06584811210632324`、`target_pos_source_pregrasp_distance=0.13803255558013916`、`contact_force_arm_body7_8_norm=[47.13663101196289,42.173789978027344]`、`handle_contact_force_norm=[47.13663101196289,42.173789978027344]`、`squeeze_y=[27.481388092041016,-42.15833282470703]`；env1 terminal `target_pos_source_handle_distance=0.07244545221328735`、`target_pos_source_pregrasp_distance=0.14452560245990753`、`contact_force_arm_body7_8_norm=[32.3857421875,49.288299560546875]`、`handle_contact_force_norm=[32.3857421875,49.288299560546875]`、`squeeze_y=[18.443986892700195,-49.27069091796875]`。本次 `handle_contact_force_norm` 与 `contact_force_arm_body7_8_norm` 相等，handle-specific sensor/body mapping 在 terminal frames 上没有明显 mismap；但 open gripper 同时出现大 contact force/squeeze，说明当前 contact/squeeze predicate 对 open-gripper collision/contact spike 过于宽松，且 complete 缺少 last-stage gate。
- 2026-06-24 17:36 HKT source fact: A2 terminal orientation diagnostics 走既有 pre-reset capture path：`LeggedRobotBase._post_physics_step()` 在 `_check_termination()` / `_compute_reward()` 后、`reset_envs_idx()` 前调用 `_capture_terminal_diagnostics()`，再由 eval completion pop 入 `episode_terminal_diagnostics`。缺 pending diagnostics 会 fail-fast，而不是用 reset 后状态补写。
- 2026-06-24 17:36 HKT source fact: `_get_a2_terminal_diagnostics()` 新增 orientation fields：`pregrasp_opening_alignment`、`pregrasp_approach_alignment`、`handle_opening_alignment`、`handle_approach_alignment`、`gripper_source_quat_w`、`gripper_source_axes_w`、`target_quat_source_handle`、`target_quat_source_pregrasp`、`target_axes_source_handle`、`target_axes_source_pregrasp`、`target_pos_source_handle`、`target_pos_source_pregrasp`。pregrasp alignment 复用 `_get_a2_gripper_handle_orientation_metrics()` 的 exact semantics：`target_quat_source[:, 1, :]` 的 target local `Y/Z` axes 映射到 source frame 后分别计算 opening/approach alignment；handle alignment 对 `target_quat_source[:, 0, :]` 使用同一公式。
- 2026-06-24 17:36 HKT source fact: diagnostics 对 `target_frame_names == ['handle', 'pregrasp']`、`target_quat_source.shape == (num_envs, 2, 4)`、`source_quat_w.shape == (num_envs, 4)` 做 exact fail-fast check；没有 silent fallback、target reorder、repeat/trim 或 try/except 掩盖。
- 2026-06-24 17:41 HKT runtime validation: `stage2_finished-20260623_221222/model_step_004000.pt` 已复跑到 `logs_eval/a2_stage0_2_eval_stage2_finished_orientation_diag_ckpt4000`；`metrics_eval.json` 可用 `/home/baoquanc/anaconda3/envs/isaaclab/bin/python -m json.tool` 解析。两个 720p mp4 分别为 `2026-06-24_17-40-06_env0000_episode0000_len83_reason-complete+upper_dof_overspeed.mp4`（84 frames, `(720,1280,3)`, fps 20）与 `2026-06-24_17-40-06_env0001_episode0000_len108_reason-complete.mp4`（109 frames, `(720,1280,3)`, fps 20）。
- 2026-06-24 17:41 HKT runtime validation: top-level metrics 为 `completed_episodes=2`、`episode_lengths=[83,108]`、`episode_terminal_reasons=["complete+upper_dof_overspeed","complete"]`、`episode_max_stage_reached=[1,1]`、`episode_goal_reached=[true,true]`；`episode_terminal_diagnostics` length 为 2，且所有新增 orientation fields 均存在。
- 2026-06-24 17:41 HKT orientation diagnosis: env0 terminal 仍在 `stage_buf=1`、`episode_length_buf=83`、`arm_j7_j8_pos=[0.03500000014901161,-0.03500000014901161]`、`gripper_primitive_raw=[1.018043041229248]`；handle/pregrasp distance 为 `0.06584811210632324` / `0.13803255558013916`，`handle_contact_force_norm=[47.13663101196289,42.173789978027344]`，`squeeze_y=[27.481388092041016,-42.15833282470703]`。env1 terminal 仍在 `stage_buf=1`、`episode_length_buf=108`、`arm_j7_j8_pos=[0.03500000014901161,-0.03500000014901161]`、`gripper_primitive_raw=[0.40541040897369385]`；handle/pregrasp distance 为 `0.07244545221328735` / `0.14452560245990753`，`handle_contact_force_norm=[32.3857421875,49.288299560546875]`，`squeeze_y=[18.443986892700195,-49.27069091796875]`。
- 2026-06-24 17:41 HKT orientation diagnosis: env0 `pregrasp_opening_alignment=handle_opening_alignment=0.8107403516769409`、`pregrasp_approach_alignment=handle_approach_alignment=-0.0752173662185669`，`target_pos_source_handle=[-0.035629548132419586,-0.016292842105031013,-0.05292496830224991]`，`target_pos_source_pregrasp=[-0.1170055940747261,0.04133845493197441,-0.06044675409793854]`。env1 `pregrasp_opening_alignment=handle_opening_alignment=0.8136235475540161`、`pregrasp_approach_alignment=handle_approach_alignment=-0.03936755657196045`，`target_pos_source_handle=[-0.04314582049846649,-0.01567736268043518,-0.056044645607471466]`，`target_pos_source_pregrasp=[-0.12447668612003326,0.042372703552246094,-0.05998130887746811]`。
- 2026-06-24 17:41 HKT diagnosis: handle 与 pregrasp alignment 数值完全相同，因为当前 pregrasp target 只带 positional offset，quaternion 与 handle target 相同。`opening_alignment ~= 0.81` 勉强满足当前 stage1 opening threshold，但 `approach_alignment` 为负且接近 0（约 92-94 degree approach-axis mismatch），说明 terminal complete 触发时 gripper 并未处在有效 stage1 orientation。这支持用户观察：angled open-gripper geometry 可以在真实 grasp/close 前撞到 handle 并产生 two-sided contact/squeeze。
- 2026-06-24 18:17 HKT source fact: raw `grasp_target` identity frame 约定为 old X = door normal / approach、old Y = handle tangent、old Z = vertical；A2 target offset rot `(0.5,0.5,0.5,0.5)` 映射为 target X = old Y、target Y = old Z、target Z = old X。`handle` 与 `pregrasp` 都使用该 rot，`pregrasp` 继续保留 pos `(0,0,0.10)`。
- 2026-06-24 18:17 HKT source fact: A2 handle contact sensor 使用 handle prim_path 作为 sensor body，`arm_body7/arm_body8` 作为 filters；IsaacLab `ContactSensor` filtered history contract 是 `force_matrix_w_history` shape `(N,H,num_bodies,num_filters,3)`，因此本 sensor expected shape 为 `(num_envs,H,1,2,3)`，helper 返回 `(num_envs,H,2,3)`。`stage2_grasp_contact_history_length: 5`，sensor reset 会把 history 清零，能阻止 reset 后 immediate false positive。
- 2026-06-24 18:17 HKT source fact: A2 `_stage_2_to_complete_condition()` 使用 `force_matrix_w_history`，要求 history 中每个 sample 都满足 both contact force `>1.0`、source-local Y abs squeeze `>0.5`、两侧 opposite sign，并显式 gate `stage_buf == STAGE_GRASP`。因为 stage advance 发生在 `_post_compute_observations_callback()`，complete predicate 还必须 gate `actual_time_in_stage_buf >= H - 1`，避免刚进 stage2 时 history 混入 stage1 samples；不要使用 `time_in_stage_buf`，它会受 `award_remaining_time_on_advance=True` 影响变负。
- 2026-06-24 18:17 HKT source fact: `StagedTaskBase._check_termination()` 的 final complete 已 gate 到 last stage，`reset_on_complete_delay` 仍只在 last-stage complete 后生效；full A2 6-stage 下 stage2 completion 不再能直接触发 episode complete。
- 2026-06-24 18:26 HKT runtime validation: 旧 checkpoint `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/stage2_finished-20260623_221222/model_step_004000.pt` 已用 `num_envs=2`、`eval_num_envs_episodes=true`、720p render 复跑到 `logs_eval/a2_stage0_2_eval_stage2_finished_grasp_frame_history_fix_ckpt4000`；`metrics_eval.json` 可用 `python -m json.tool` 解析。结果为 `completed_episodes=2`、`episode_lengths=[21,250]`、`episode_terminal_reasons=["upper_dof_overspeed","stage_overtime"]`、`episode_max_stage_reached=[0,0]`、`episode_goal_reached=[false,false]`、`episode_rewards=[-22.020288467407227,-29.03823471069336]`，`episode_terminal_diagnostics` length 为 2。
- 2026-06-24 18:26 HKT runtime diagnostics: diag0 为 `env_id=1`、`stage_buf=0`、`time_in_stage_buf=21`、`episode_length_buf=21`、`terminal_reasons=upper_dof_overspeed`、`pregrasp_opening_alignment=0.8847`、`pregrasp_approach_alignment=0.5954`、handle/pregrasp distance `1.1629/1.1497`、`gripper_primitive_raw=-1.9915`。diag1 为 `env_id=0`、`stage_buf=0`、`time_in_stage_buf=250`、`episode_length_buf=250`、`terminal_reasons=stage_overtime`、`pregrasp_opening_alignment=0.9168`、`pregrasp_approach_alignment=0.4261`、handle/pregrasp distance `0.1867/0.2293`、`gripper_primitive_raw=-1.9207`。mp4 均存在：env0000 len250 reason-stage_overtime、env0001 len21 reason-upper_dof_overspeed，均为 720p、fps 20。
- 2026-06-24 18:26 HKT runtime diagnosis: 旧 false-success checkpoint 在新 target frame + last-stage/history gate 下不再 stage1 complete，`goal_reached=false` 且 `max_stage_reached=0`；runtime 没有触发 sensor history 或 target frame fail-fast。该 checkpoint 不应 resume，后续需要重新训练。
- 2026-06-24 18:26 HKT PPO smoke validation: quick exp 用 `num_envs=2`、`algo.trl.num_total_batches=2`、`algo.config.num_steps_per_env=8`、`algo.config.num_mini_batches=1`、`algo.config.num_learning_epochs=1`、`save_interval=9999`，project 为 `a2_piper_stage0_2_grasp_terminal_a2_base_smoke`，experiment 为 `door_open_a2_base_stage0_2_grasp_terminal_lstm_smoke_history_fix`。env create/reset 与 scene sensors initialization 完成，training loop 打印 `Learning iteration 1` 和 `Learning iteration 2`，actor/critic obs dims 保持 130/135，`Env/average_stage_reached=0`、`Env/average_goal_reached=0`、`Env/average_last_stage_goal_reached=0`；无 target frame/contact history crash。tiny `2x8` rollout 没有 episode completion，因此 mean rewards/length 为 `nan`，属于该 smoke 的 empty episode statistics。训练 iteration 完成后 IsaacSim shutdown hang，需要 Ctrl-C；main-agent 已发送 Ctrl-C，session exit code 0。
- 2026-06-24 21:52 HKT runtime validation: 新训练 checkpoint `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/stage2_finished_orientation-20260624_183146/model_step_001000.pt` 已用 `num_envs=2`、`eval_num_envs_episodes=true`、720p render eval 到 `logs_eval/a2_stage0_2_eval_stage2_finished_orientation_ckpt1000_720p`；`metrics_eval.json` 可解析，两个 mp4 均为 720p、fps 20。结果为 `completed_episodes=2`、`episode_lengths=[452,452]`、`episode_terminal_reasons=["stage_overtime","stage_overtime"]`、`episode_max_stage_reached=[2,2]`、`episode_goal_reached=[false,false]`、`episode_rewards=[46.406917572021484,41.90083312988281]`。
- 2026-06-24 21:52 HKT runtime diagnostics: terminal diagnostics 显示 env0/env1 都在 `stage_buf=2`、`time_in_stage_buf=100` overtime；pregrasp opening/approach alignment 分别为 `0.9858/0.9551` 与 `0.9896/0.9773`，handle distance 为 `0.0140/0.0154`，pregrasp distance 为 `0.1066/0.0984`。terminal `handle_contact_force_norm=[0.0,0.0]`、`squeeze_y=[0.0,0.0]`，`arm_j7_j8_pos` 约为 `[0.035,-0.03]` / `[0.035,-0.024]`，说明当前 checkpoint 已学到进入 stage2 并对准 handle 附近，但还没有形成 stable contact/squeeze 或 true close grasp。
- 2026-06-24 22:11 HKT source fact: `LeggedRobotBase._post_physics_step()` 在 `_check_termination()` / `_compute_reward()` 后、`reset_envs_idx()` 前调用 optional A2 trace hook；因此 stage2 terminal/overtime 最后一帧仍在 reset 前 capture，且 reset 后状态不会写入同一条 trace。trace hook 在 non-A2 或 non-eval 时 return；A2 eval 若未 init trace buffer 会 fail-fast；已 init 后只在 `stage_buf == STAGE_GRASP` 时 append record。
- 2026-06-24 22:11 HKT source fact: trainer eval 仅在 `env._use_a2_base` 为 true 时初始化并写出 `eval_output_dir/stage2_step_trace.json`；non-A2 eval 不写该文件。JSON 写入复用 `_make_json_safe(..., path="stage2_step_trace")`，通过 `.tmp` file + `os.replace()` 原子替换，未知 object type 继续 fail-fast。
- 2026-06-24 22:19 HKT runtime validation: `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/stage2_finished_orientation-20260624_183146/model_step_001000.pt` 已复跑到 `logs_eval/a2_stage0_2_eval_stage2_finished_step_trace_ckpt1000/`；`metrics_eval.json` 与 `stage2_step_trace.json` 均可用 `python -m json.tool` 解析，两个 mp4 均输出为 720p。metrics 仍为 lengths `[452,452]`、terminal reasons `["stage_overtime","stage_overtime"]`、max stage `[2,2]`、goal reached `[false,false]`。
- 2026-06-24 22:19 HKT runtime diagnosis: step trace 共 272 条 stage2 records。env0 有 122 条，primitive open/close 为 `117/5`、sign switches `1`、handle distance min `0.0105m`、frames `<0.02m` 为 `113`、contact/squeeze/complete-predicate frames 均为 `0`。env1 有 150 条，primitive open/close 为 `138/12`、sign switches `15`、`abs(primitive)<0.05` 为 `108`、handle distance min `0.0103m`、frames `<0.02m` 为 `131`、contact/squeeze/complete-predicate frames 均为 `0`。env1 确认存在 grasp-target 附近 open/close oscillation，但没有 stable contact/squeeze。
- 2026-06-24 22:45 HKT source fact: A2 close reward strict gate 固定为 `stage_buf == STAGE_GRASP`、handle distance `<0.03m`、`opening_alignment >= 0.9`、`approach_alignment >= 0.9`，orientation metrics 复用 `_get_a2_gripper_handle_orientation_metrics()`。gate 对 `stage_buf` 与 `target_pos_source` shape fail-fast；reward methods 在 non-A2 env 直接 `RuntimeError`。
- 2026-06-24 22:45 HKT source fact: `a2_stage2_close_progress` 对 `_a2_gripper_dof_indices`、open/close target、`simulator.dof_pos` 做 fail-fast validation；open-close span 为 0 会 `RuntimeError`，不使用 `clamp_min` fallback。progress formula 不做 per-DOF clamp，只在 two-DOF mean 后按 `/0.6` clamp。
- 2026-06-24 22:45 HKT static validation: `py_compile` 通过；Hydra full A2 compose resolved close scales `1.0/0.5`、reward routing 各出现一次、stage list length `6`；Hydra quick compose resolved same close scales/routing、stage list length `3`；no-sim formula sanity 覆盖 command threshold 与 progress mean-clamp behavior。
- 2026-06-24 22:49 HKT runtime validation: close shaping smoke 使用 quick exp、`num_envs=2`、`num_total_batches=1`、`num_steps_per_env=8` 跑到 `Learning iteration 1`；训练日志确认两项新 reward 在 reward scale 与 penalty reward names 中注册，并输出 mean episode reward lines。训练主体完成后 IsaacSim shutdown hang，main-agent Ctrl-C 后 exit code 0。
- 2026-06-24 22:54 HKT source fact: `penalty_base_roll_pitch_l2` 使用 actual `self.rpy`，要求 shape `(num_envs, >=2)`，返回 `roll^2 + pitch^2`；stage decorator 限定 stage0/1，因此 stage2 grasp 不受该 upright penalty 直接作用。该项未加入 `reward_penalty_reward_names`，保持 fixed scale `-2.0`，与现有 `penalty_face_door` / `penalty_not_standing_still` 等 fixed penalty 风格一致。
- 2026-06-24 22:54 HKT validation: `py_compile`、targeted `git diff --check` 通过；Hydra quick compose resolved 3-stage lists 且 `penalty_base_roll_pitch_l2: -2.0`，Hydra full A2 compose resolved 6-stage lists 且同样启用该 scale；no-sim tensor formula sanity 验证 `[0,0] -> 0`、`[0.1,-0.2] -> 0.05`、`[0.3,0.4] -> 0.25`。

## Implementation Boundary

- 优先新增独立 experiment/env config，例如 stage0-2 quick test config，而不是直接改默认 full 6-stage config。
- 3-stage config 必须同步修改 `max_stage_time`、`stage_reward_scale`、`staged_reset_ratios` 等长度相关字段，保持 fail-fast shape/assert 行为。
- `reset_on_complete_delay` 可设为 `0` 或短 delay；若需要观察 grasp 后稳定性，再保留小 delay。
- Stage3+ reward terms 可以在 quick test config 中关闭或不进入，但不要把 open/swing/through reward 从 shared reward implementation 中删除。
- 训练产物、checkpoint 与日志需要用独立 experiment name/project name，避免和 full door-open 6-stage result 混用。

## TODO Summary

- 2026-06-24 22:54 HKT - A2 Stage2 Close Shaping Rewards 与 stage0/1 base roll/pitch upright penalty 已完成 static validation；下一步需要用新 reward retraining，继续观察 stage occupancy、stage0/1 roll/pitch magnitude、stage2 close command/progress、contact/squeeze complete、overtime reset、termination frequency 与 `average_goal_reached`。
- 2026-06-24 22:45 HKT - true close/aperture condition 或 complete predicate 强化仍未实施；本轮只加 close shaping rewards，不应混入 contact history gate、stage transition、reset、camera、render timing 或 action semantics 修改。

## DONE Summary

- 2026-06-22 20:29 HKT - 创建独立 memory entry `stage0-2-grasp-terminal`，记录 quickTEST 分支的 stage0-2-only training 目标、stage2 terminal success 语义、与 full 6-stage task 的边界。
- 2026-06-22 20:42 HKT - 新增独立 stage0-2 quick test config，并完成 Hydra static validation：3-stage list values、reset delay 0、stage3+ reward scales 0.0、`obs_dims.stage: 3` 均符合计划。
- 2026-06-22 21:23 HKT - 完成 frozen A2_Base TorchScript non-registered trainer fix review：wrapper 不再把 TorchScript model 注册进 module tree，optimizer scan old blocker 消失，bounded smoke 已推进到 training/reset observation 阶段。
- 2026-06-22 21:31 HKT - 完成 `piper_gripper_handle_frame_transformer` target order 修复：`py_compile` 通过，single-GPU smoke 未再触发 `['pregrasp', 'handle']` mismatch，并推进到 `_homie_history_length` reset observation blocker。
- 2026-06-22 21:35 HKT - 完成 `_homie_history_length` reset observation 修复：`py_compile` 通过，single-GPU smoke 已越过 `_get_obs_a_history_homie()`，并推进到 `ResetFromDataset.reset_count` blocker。
- 2026-06-22 21:39 HKT - 完成 G1 `ResetFromDataset` disable fix：quick config 显式 `reset_from_dataset.enabled: False`，smoke 不再加载 LAFAN motion files，并推进到 PPO forward shape blocker。
- 2026-06-22 22:07 HKT - 完成 PPO recurrent A2_Base injection shape mismatch 修复 review：确认 `a2_base_obs` unsplit 后与 `high_level_actions` 对齐，PPO loss tensors 未切换到 padded layout，fail-fast shape validation 无隐藏 fallback；worker 和 main-agent small smoke 均已到 `Learning iteration 1`。
- 2026-06-22 22:52 HKT - 完成 `OrderedTargetFrameTransformer` multi-env duplicate 修复 review：duplicate check 限定为 config-level target frame names，multi-env 同名 target 不再误判；cfg target order 仍为 `handle -> pregrasp`，true config duplicate 继续 fail-fast。
- 2026-06-22 23:53 HKT - 完成 WebRTC training visualization route diagnosis：结合 static visual alignment memory，确认 full GUI/static preview 是 preview-only workflow；stage0-2 training 可走 IsaacLab `AppLauncher` livestream env path，但应使用 single-process visual run，不直接叠加到 multi-rank PPO 长训。
- 2026-06-22 23:59 HKT - 完成 WebRTC no-ready diagnosis：当前问题不是 `PUBLIC_IP` env 缺失，而是 stream service 未 start；用 `ss`/livestream log 作为 readiness 判据，并建议显式使用 headless rendering kit 规避 full GUI/zenity path。
- 2026-06-23 00:07 HKT - 完成 visual training toolbar blocker 修复：`train_agent_trl.py` 在 AppLauncher 创建前安装 exact `omni.kit.widget.toolbar` guard，允许 full GUI/WebRTC path 越过缺失 optional toolbar widget；验证通过 `py_compile` 与 `git diff --check`。
- 2026-06-23 00:13 HKT - 完成 post-toolbar WebRTC diagnosis：当前不是 toolbar import crash；环境里 stale visual/training processes 叠加导致 GPU/Kit/livestream 状态不可判定，下一步应先终止旧 visual PIDs，再单独复跑一个 `LIVESTREAM=1` visual run 并检查 `49100`/livestream start event。
- 2026-06-23 00:19 HKT - 记录 GUI route decision：完整 GUI 调试优先走 `xpra`/remote desktop 提供 `DISPLAY`，训练命令改为 `headless=False` 且不设置 `LIVESTREAM`；WebRTC route 暂作为非首选。
- 2026-06-23 13:41 HKT - 完成 xpra startup diagnosis：HTML endpoint `http://10.120.16.39:14500/` 可由 xpra web server 提供，但当前 display `:100` 为 Xvfb/llvmpipe software renderer，不能作为 Isaac Sim full GUI/Vulkan 调试目标；需要 GPU-backed Xorg/VirtualGL/TurboVNC/NoMachine 或继续用 headless eval render mp4。
- 2026-06-23 19:57 HKT - 记录 checkpoint visualization command route：对 `model_step_004000.pt` 使用 single-process headless eval，开启 `simulator.config.render_results=true` 并指定 `env.config.save_rendering_dir`，输出 mp4 与 `metrics_eval.json` 到独立 `logs_eval` 目录；训练曲线图已存在于 `logs_rl/a2_piper_stage0_2_grasp_terminal_a2_base/local_visualizations/`。
- 2026-06-23 20:03 HKT - 记录 A2 eval smoke 结果：eval 主流程已跑完 2 episodes 并生成 mp4，证明 A2 eval action path 基本可运行；当前 blocker 是 `eval_dict` JSON serialization 与 observer-camera 可用性，下一步应先修 `metrics_eval.json` safe serialization，再调整可视化 camera/episode timing。
- 2026-06-23 20:12 HKT - 完成 A2 eval metrics JSON serialization 修复：`metrics_eval.json` 写入前进行 fail-fast JSON-safe conversion，tensor/numpy metrics 可写出，未知对象类型仍报 key path；最终代码版本复跑 `model_step_004000.pt` 2-episode eval smoke 成功完成并可用 `python -m json.tool` 解析 metrics。
- 2026-06-23 20:33 HKT - 完成 A2 Eval True-Episode Rendering Fix review：确认 per-env mp4 writer 不跨 episode stitching，terminal frame 在 reset 前 capture，restart initial frame 在 reset 后 capture；diagnostics summary 增加 episode length/reward/goal/max-stage/terminal-reason arrays，未改 reward、stage transition、termination threshold、`reset_on_complete_delay` 或训练 config 语义。验证通过 `py_compile`、targeted `git diff --check` 与 no-IsaacSim helper smoke；仍待 main-agent 复跑 IsaacSim runtime smoke 检查实际 mp4 画面质量。
- 2026-06-23 20:44 HKT - 完成 true-episode render runtime smoke 与尾巴 mp4 修复：复跑 `model_step_004000.pt` 2-env eval 后只保留两个正式 episode mp4，帧数满足 initial+terminal contract，`metrics_eval.json` 包含 length/reason/stage/goal diagnostics；当前 early termination reason 是 `bad_orientation`。
- 2026-06-23 21:15 HKT - 完成 A2 Door Reset Stabilization review 与 runtime smoke：A2 reset branch 符合 `base_init_state + env_origins`、x/y/yaw randomized、root/DOF vel zero 的 contract，target-state path 保持 parent/fail-fast，non-A2/G1 reset 逻辑未变；2-env checkpoint eval 不再 4/5 step `bad_orientation`，改为 stage2 `stage_overtime` at length 452。
- 2026-06-23 21:27 HKT - 完成 door-top-down eval camera patch review：`door_top_down` 使用 door root state anchor，default `eval_rendering` 改为 top-down door-relative offsets，missing key / unknown mode 继续 fail-fast；静态验证通过 `py_compile` 与 targeted `git diff --check`，未跑完整 IsaacSim runtime eval。
- 2026-06-23 21:50 HKT - 完成 door-top-down runtime camera diagnosis/fix：确认原 patch config 已 resolve 但 `TiledCamera` render product 未消费 Fabric-only pose write；`render_results()` 已启用 USD sync 后再 render/update/read RGB。extreme camera probe 证明视角可改变；`[0.05,0,2]` probe 证明 top-down 生效但视野过近。
- 2026-06-23 21:50 HKT - 完成 eval camera default tuning：把 default door-root anchored camera 从近距离垂直 top-down 改为已验证的斜俯视 offsets，便于同时观察 robot、door 与 handle。
- 2026-06-23 21:58 HKT - 完成 rendering cleanup 与 full oblique eval recording：旧 rendering/previews 已清空，新完整录制保存在 `logs_eval/a2_stage0_2_eval_oblique_full_ckpt4000/`，metrics 与 mp4 frame count 已验证。
- 2026-06-23 22:10 HKT - 完成 eval camera 720p 默认分辨率与重录：`eval_camera_resolutions` 默认 `[720,1280]`，旧 `logs_eval` 内容已按用户确认清空，新 720p 完整录制保存在 `logs_eval/a2_stage0_2_eval_oblique_720p_ckpt4000/`。
- 2026-06-24 16:35 HKT - 完成最新 `stage2_finished-20260623_221222/model_step_004000.pt` 的 720p 斜俯视 eval recording：输出保存在 `logs_eval/a2_stage0_2_eval_stage2_finished_720p_ckpt4000/`，两个 episode 均 goal reached，terminal reasons 分别为 `complete+upper_dof_overspeed` 与 `complete`。
- 2026-06-24 16:40 HKT - 完成 latest 720p eval grasp semantics 诊断：视频中未见 gripper 闭合与 metrics `episode_max_stage_reached=[1,1]` 一致，当前 `complete` 由未 gate 到 final stage 的 `_stage_2_to_complete_condition()` contact/squeeze predicate 触发；该结果不能作为真实 stage2 close-grasp success。
- 2026-06-24 17:19 HKT - 完成 A2 Eval Terminal Diagnostics runtime validation：`metrics_eval.json` 可解析，两个 720p mp4 frame count 与 terminal reason 命名符合 true-episode rendering contract，`episode_terminal_diagnostics` length 为 2 并与 `episode_lengths/episode_terminal_reasons` 对齐。
- 2026-06-24 17:19 HKT - 完成 terminal diagnostics 诊断：terminal success 发生在 `stage_buf=1`，gripper DOFs 仍处于 open target 且 primitive raw 为 positive open command；handle-specific force mapping 未见明显 mismap，但 large contact force/squeeze 说明当前 contact/squeeze complete predicate 对 open-gripper collision/contact spike 过宽，后续仍需单独修复 last-stage gate、gripper close/aperture 或 close-command 条件，以及 A2 stage2 gripper close shaping。
- 2026-06-24 17:36 HKT - 完成 A2 Terminal Orientation Diagnostics code implementation + static review：确认新增 orientation diagnostics 字段、pre-reset capture path、alignment semantics 与 exact fail-fast shape/order contract；`py_compile` 与 targeted `git diff --check` 均通过。runtime orientation eval 仍待 main-agent 执行。
- 2026-06-24 17:41 HKT - 完成 A2 Terminal Orientation Diagnostics runtime eval：`metrics_eval.json` 可解析，两个 720p mp4 frame count/fps/shape 符合 expected rendering contract，`episode_terminal_diagnostics` length 为 2 且所有新增 orientation fields 存在。
- 2026-06-24 17:41 HKT - 完成 terminal orientation 解释：runtime output 与之前 terminal diagnostics 是同一 false-success mode，complete 仍在 `stage_buf=1` 触发，gripper DOFs 仍为 open target，primitive raw 仍为 positive open command；handle/pregrasp alignment 相同来自 shared quaternion，approach alignment 为负且接近 0，说明 gripper approach-axis mismatch 约 92-94 degree，支持 angled open-gripper collision/contact spike 诊断。
- 2026-06-24 18:17 HKT - 完成 A2 Grasp Frame + Contact History Complete Fix implementation/static review：确认 target frame rot、ContactSensor history shape contract、A2 history all-sample predicate、last-stage final complete gate 与 `reset_on_complete_delay` preservation；reviewer 追加 `actual_time_in_stage_buf >= H - 1` gate，防止 stage2 首几步 history 混入 stage1 samples。验证通过 `py_compile`、Hydra quick/full compose 与 no-sim quaternion/history predicate probe；尚未跑 IsaacSim runtime eval 或 PPO smoke。
- 2026-06-24 18:26 HKT - 完成 A2 Grasp Frame + Contact History Complete Fix old-checkpoint runtime eval：旧 `stage2_finished-20260623_221222/model_step_004000.pt` 输出到 `logs_eval/a2_stage0_2_eval_stage2_finished_grasp_frame_history_fix_ckpt4000`，`metrics_eval.json` 可解析，两个 720p mp4 存在且 fps 20。结果为 lengths `[21,250]`、terminal reasons `["upper_dof_overspeed","stage_overtime"]`、max stages `[0,0]`、goal reached `[false,false]`；旧 stage1 false-success 不再出现，且未触发 sensor history/target frame fail-fast。结论：该旧 checkpoint 不应 resume，必须重新训练。
- 2026-06-24 18:26 HKT - 完成 A2 Grasp Frame + Contact History Complete Fix small PPO smoke：quick exp 以 `num_envs=2`、`num_total_batches=2`、`num_steps_per_env=8` 跑到 `Learning iteration 1/2`，actor/critic obs dims 保持 130/135，`Env/average_stage_reached=0`、`Env/average_goal_reached=0`、`Env/average_last_stage_goal_reached=0`；无 target frame/contact history crash。tiny rollout 没有 completed episodes，mean rewards/length 为 `nan` 属 empty episode statistics；training iterations 后 IsaacSim shutdown hang，Ctrl-C 后 exit code 0。
- 2026-06-24 21:52 HKT - 完成最新 `stage2_finished_orientation-20260624_183146/model_step_001000.pt` 的 720p eval recording：输出保存在 `logs_eval/a2_stage0_2_eval_stage2_finished_orientation_ckpt1000_720p/`，两个 episode 均为 `stage_overtime` at length 452，max stage reached `[2,2]`，goal reached `[false,false]`；metrics 可解析，两个 mp4 均为 720p、fps 20。terminal diagnostics 显示 orientation/handle distance 已较好，但 handle contact force 与 squeeze 均为 0，当前仍缺 stable contact/close grasp。
- 2026-06-24 22:11 HKT - 完成 A2 stage0-2 eval-only per-step trace diagnostics implement review：确认 trace 是 A2-only / eval-only / stage2-only，字段复用 terminal diagnostics，reset 前 capture 不漏 stage2 terminal/overtime 最后一帧，JSON 通过 temp file + `os.replace()` 写出；reviewer 追加 A2 eval missing-init fail-fast 修正；未发现 reward、stage transition、complete predicate、reset、camera 或 render timing 改动。
- 2026-06-24 22:19 HKT - 完成 ckpt1000 stage2 per-step trace runtime diagnosis：输出保存在 `logs_eval/a2_stage0_2_eval_stage2_finished_step_trace_ckpt1000/`，`stage2_step_trace.json` 可解析。env1 在 handle distance 约 `1-2cm` 时出现 `gripper_primitive_raw` 近零反复跨符号、`arm_j7/j8` aperture 随之开合；但两个 env 的 handle contact force、squeeze 与 complete predicate frames 全程为 0，因此当前问题不是 close grasp 被 handle 阻挡后的稳定夹持，而是到达 grasp target 后没有形成物理接触且 gripper close command 不稳定。
- 2026-06-24 22:45 HKT - 完成 A2 Stage2 Close Shaping Rewards implement review：确认新增 `a2_stage2_close_command` 与 `a2_stage2_close_progress`，strict gate 为 stage2 + handle distance `<0.03m` + opening/approach alignment `>=0.9`；reviewer 最小修正了 command literal formula、progress mean-clamp order、zero span fail-fast 与 gripper DOF index validation。验证通过 `py_compile`、Hydra quick/full compose 与 no-sim formula sanity。
- 2026-06-24 22:49 HKT - 完成 A2 Stage2 Close Shaping Rewards bounded PPO smoke：quick exp 跑到 `Learning iteration 1`，reward functions 正常注册并输出 `rew_a2_stage2_close_command` / `rew_a2_stage2_close_progress`，无 reward shape 或 rollout crash；tiny `2x8` stage0-only smoke 中两项 reward 为 0 属 expected，训练主体完成后 IsaacSim shutdown hang，Ctrl-C 后 exit code 0。
- 2026-06-24 22:54 HKT - 完成 stage0/1 actual base roll/pitch upright penalty：新增 `penalty_base_roll_pitch_l2: -2.0`，reward helper 直接返回 `self.rpy[:,0:2]` 的 L2 并对 rpy shape fail-fast；验证通过 `py_compile`、targeted `git diff --check`、Hydra quick/full compose 与 no-sim formula sanity。

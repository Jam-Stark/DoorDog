# N01 预研：完整 C002 上的恢复机制与 Teacher→Student 能力传递

2026-09-20 HKT。整理：Codex planner。状态：**Owner 已提出研究问题；方法方案待 Pro 独立建议，未实施、未运行新实验。**

输入底座为 `v29-c002-baseline`，保留 B05。对应交付入口：`scriptsFORhuman/v29/pro_handoff/20260920_n01_recovery_transfer/`。原话保存在 `OWNER_REQUEST.md`；实际代码与证据见 `LOCAL_FACTS.md`、`SOURCE_INDEX.md` 与交付 manifest。

## 研究目标

请给出一套从恢复任务定义、Teacher学习、Student采样/监督，到独立Student评估的连贯方案。我们需要两个分别成立的结果：Teacher能从可恢复失败中继续完成任务；Student在自身感知、动作误差与闭环状态分布下也能恢复。前者不能自动推出后者。

先基于 Owner 问题、当前 C002 事实和你独立找到的一手研究形成初判，再读历史 novelty 资料。历史提案仅供反驳、借鉴和避免重复实验。请列明哪些结论来自代码/实际实验，哪些是文献结论，哪些是你推荐的工程假设。

## 1. 重新定义恢复图，而非给每个 stage 添加一条退边

从物理任务和可观测性出发判断需要恢复的情形。至少讨论接近/站位偏差、预抓对准失败、夹闭失败或单指接触、握持后滑移/失抓、压柄/解锁失败、开门时被回关负载打断、正常释放后门继续运动与通行受阻；这些是待筛选的情形，不要求把每项都实现为独立节点。

明确区分正常释放和非计划失抓、短时接触闪断和真实失去约束、可恢复的任务失败和应停止的失败。仿真API错误、无效tensor或损坏状态仍应 fail-fast，不属于学习恢复的目标。

给出最小充分的节点/状态定义和有向图。图的节点可以是可执行技能、物理状态区域或策略内部状态，不必与现有Stage0–5一一对应。对你推荐的每条恢复边说明：

- 触发证据由谁看到；Teacher可用真值和部署Student可用信号分别是什么。
- “退回哪里”具体指重新建立哪些物理条件；何时原地重握足够，何时需退至pregrasp、调整base、重新找handle或结束本次尝试。
- 进入目标状态需要什么动作和完成条件；门、机器人、contact、时间与策略记忆如何连续演化。
- 是否改变stage标签、动作积分/目标、contact streak、历史最高stage、计时、reward和RNN state；哪些应保留、哪些需要重建，为什么。
- 重复失败后怎样决定继续、换目标或终止；给有物理意义的初始方案，而不是无依据地固定重试次数。

不得把episode reset/snapshot恢复、训练状态采样、在线策略恢复混为一件事。训练时可以从失败状态初始化，但部署时不能瞬移回历史状态。比较固定回到Stage2、条件选择落点、以及不显式建图的同等感知recurrent policy；若图不是合适的方法，请直接解释并提出更简单的替代。

历史上“强回弹后重抓把手扶门”被暂放N02。此次Owner要求重新研究完整恢复图：请分析行为恢复与交互历史适应的分工，必要时建议重分边界；不要把旧归属当禁区，也不要未经Owner采纳就宣布范围已转移。

## 2. Teacher怎样学到真实且有效的恢复能力

设计具有因果含义的失败暴露和课程。区分自然出现的失败、动作/外力/感知扰动、从真实失败轨迹抽取状态、以及可能破坏动力学或可达性的人工state edits。给出首先值得做的最小范围，说明扰动施加给谁、作用于什么量、发生时机、持续时间、幅度、撤除条件以及如何读回实际效果。

要求Teacher接触“能恢复但不是太容易”的状态，同时说明Teacher能力边界之外的失败如何处理。不要将扰动命令发出、手指张开、contact瞬时缺失或成功重新接触分别当成同一个事件。避免只在已经成功握住且即将成功的episode里采样而漏掉Student常见的失败。

说明自然任务样本与恢复样本的配比/课程、reward或训练目标怎样鼓励最终完成、是否会通过主动失抓反复赚进展奖励、是否保留累计任务时间。给初始工程数值时注明依据和待本地校准的量，不把未验证数字变成硬性科学门槛。

即使C002最终策略还不够成熟，也请给分阶段可开展的路径：哪些可以先做source/小场景操作证明，哪些必须等到有足够交互能力的checkpoint，怎样避免让“完美Teacher”成为永远达不到的前置条件。

## 3. 核心问题：怎样让Student获得恢复能力

请先核对本项目实际DAgger代码，不要默认蒸馏永远是Teacher执行、Student只看。也不要只回答“换成DAgger”：项目已有DAgger路径，仍需论证失败状态是否被访问、Teacher在这些状态是否能给有效标签、Student是否看得到决定恢复动作的信息，以及恢复片段是否被优化目标充分使用。

分别回答三种数据来源：Teacher主导的rollout、Student闭环rollout并查询Teacher、显式构造/重放恢复训练状态。为什么某种数据来源会覆盖或漏掉部署时的失抓与恢复？若Teacher太强而自然失败少，怎样让Student仍然经历失败并承担恢复动作，而不靠Teacher频繁接管掩盖困难？

对最终推荐方案写出训练循环或伪代码：每一步谁执行动作、Teacher看到什么状态/历史、Student看到什么、何时施加扰动、何时查询/接管、写入什么数据、如何采样与计算loss、何时reset，以及何时取消Teacher参与做独立评估。需要说明：

- 在线纠正标签是否来自Student当前物理状态；Teacher的LSTM hidden如何沿Student的真实轨迹同步，不能拿正常Teacher轨迹的hidden与失败状态拼接。
- Teacher在Student引起的分布外状态不可靠时，怎样辨识有效监督、是否需要先补Teacher能力，以及如何保留这些失败而非无声丢弃。
- Student缺少stage、接触真值、门参数/目标frame时如何判断恢复；是由历史隐式推断、学习辅助状态、可部署显式检测器、共享状态机，还是其他机制；明确部署时是否残留仿真oracle。
- 仅复制每帧action是否足够传递选择恢复目标、记忆与多步恢复行为；必要时考虑动作序列、阶段/恢复目标辅助监督、轨迹级权重或Student闭环finetune，并说明增量成本及为何需要。
- 正常轨迹会不会淹没短恢复片段；数据聚合、窗口、burn-in、采样权重和同一episode的连续性怎样处理。
- snapshot采样时相机图像、动作/history、contact缓存、Teacher和Student RNN状态如何获得一致的初始条件；训练真值label可以用，部署输入不能偷带。

请比较少数真正不同的方案并选择一个推荐主线与一个必要备选，而不是平铺所有算法名称。明确当前代码可以直接复用什么，必须改变什么，和哪些建议没有现成实现。

## 4. 最小但能归因的验证路线

先证明功能路径，再用少量有判别力的对照回答两个问题：恢复Teacher是否有用；面向恢复的Student训练是否把能力传过去。需要把Teacher方法收益与Student采样/监督收益拆开，保持同等C002+B05底座、可部署感知条件及合理对齐的数据/交互预算。不要把更强Teacher、更多数据、换相机、缩窄门域和新蒸馏方法一起改后统称N01收益。

定义完整漏斗：全部自然episode、进入扰动/交互窗口、实际施加扰动、实际有效失效、尝试恢复、重建物理约束、恢复后完整任务成功。报告全部episode分母及实际失败条件下的结果，避免只统计成功注入或可恢复子集。记录名义性能代价、恢复时延、重复失败、最终失败原因，左右侧与必要的B05/门域分层即可。

评估必须由Student独立闭环、关闭Teacher接管与训练staged reset；区分部署可实现的恢复判据和使用oracle的分析读回。保留未见扰动/失效组合和自然失败评估，不能只证明记住同一个注入模板。给最小顺序pilot与继续/改方向条件，不设计庞大矩阵、不现在申请GPU/预算，也不要求实现前先写大套回归/防御测试。

## 5. 独立研究与完整交付

请独立检索一手论文、项目官方代码或作者资料，对比恢复策略/技能图、失败状态采样、DAgger及干预式模仿、特权Teacher到部分可观测Student的相关方法。区分标准工程组合与可能的新贡献；“有恢复图”本身不是novelty证据。给出核心来源及它实际支持的范围；不能访问资料时说明，不编造论文、实验或引用。

输出一个可供本地继续设计和实施的完整方案：直接回答Owner五问、推荐结构、恢复边表/图、Teacher方案、Student训练数据流与伪代码、源码落点、最小pilot、风险/未知与备选。只把真正影响选型的少量问题留给Owner，并给出合理暂定方案。

Pro回包是建议，不自动授权本地实施。完整回包格式与本地接手规则以 `PRO_REVIEW_PROMPT.md` 为准。

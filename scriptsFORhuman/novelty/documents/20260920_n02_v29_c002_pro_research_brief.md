# N02预研：从能力缺口到在线调整开门行为

2026-09-20 HKT。整理：Codex planner。状态：**Owner提出六项研究要求；待Pro独立研究/迭代和完整方案，未实现或运行N02。**

主底座为完整 `v29-c002-baseline`，保留B05。交付入口：`scriptsFORhuman/v29/pro_handoff/20260920_n02_online_adaptation/`；先读Owner原话、LOCAL_FACTS、SOURCE_INDEX与已存证据。

## 先解决什么问题

请从具体操作情形建立能力缺口，不从网络名称出发。Owner希望策略在开门过程中持续观察、结合历史、调整姿态和开门方式：普通情况尽量近中性姿态、arm主导；需要时使用roll/pitch；在可控甩门与quiet握持之间选择，强回弹且身体仍受门扇影响时维持有效握持，避免门快速回碰trunk。

这些是研究目标，不是C002已经达到的行为，也不意味着轻门必然容易、重门必然需要倾斜。请独立判断“信息/历史/训练暴露/行为可用性/奖励与控制约束”各自的作用。允许结论是无需显式估计物理参数、无需全身wrench，甚至无需新的独立网络；但必须提出能检验且能执行的替代。

先独立形成目标和最简方案，再读历史novelty与指定论文作挑战。必要时回到目标定义重做选型，说明最终方案相对初始设想改变了什么和原因；不要求固定次数的机械自评，不输出内部推理轨迹。

## 1. 用具体情形分离能力缺口

选择少量有判别力的C002情形，如：相同门重但closer/摩擦不同；同样开不动但分别因为门轴负载、handle接触滑移、B05局部几何或arm可达性；静止握持下门速接近0；接触前观察很短；释放后门回关但身体尚未通过；握持丢失后反馈突然改变。这些是待分析情形，不能直接当训练故障标签或预定模块清单。

对每种情形回答：现有Teacher133D/LSTM实际已经知道什么；Student81D＋RGB知道什么；还缺什么；多一段历史是否就能区分；如果增加信息仍不能改善，是否因为没有合适动作、握持/释放技能、训练样本或奖励支持。不要把训练还没学会等同不可观测，也不要把不唯一可辨识等同没有任何控制上有用的信息。

给一张能力缺口表，并选出首版N02最值得解决的少数情形。对暂不做的情形给理由，不将整个门操作系统同时重写。

## 2. 先选目标，再选模型

比较少数真正不同的候选，例如任务相关的短期响应/进展预测、保持/释放条件下的回关或净空趋势、交互/抓握可信度、有效负载的低维描述，以及显式物理参数或wrench。此列表只是搜索空间，不要求全部采用。

每个候选给出：

| 必要维度 | 需要回答 |
|---|---|
| 输入 | 哪些当前/历史量，Teacher/Student分别怎样得到，时间同步与缺测怎么办 |
| 目标 | 物理量、事件、未来状态、隐状态或分布；单位/坐标系/对象必须清楚 |
| 时间尺度 | 当前估计或预测多远，历史窗口/更新频率/可接受延迟是什么 |
| 动作条件 | 是否只解释已发生响应；预测未来时是否给定未来动作或候选行为 |
| 监督 | simulator真值、短rollout结果、可部署传感、自监督或任务损失；数据如何产生 |
| 部署 | 需要的输入是否已存在、待确认或必须新增；训练label不能偷偷变成部署输入 |
| 控制使用 | 估计会改变哪个动作、模式或目标；哪个决策在没有它时会做错 |

区分“观测到的相关响应”与“如果保持/释放/加姿态会怎样”的动作条件预测。若要比较动作的反事实后果，不能仅把单条已执行轨迹的未来当成所有动作的监督；请给合理的数据或短分支采样方案及最小成本。若使用uncertainty，解释它在短历史/无接触/新几何下怎样被校准与使用，而不是再加一个没有判据的置信度输出。

优先选择能改变控制且可由数据支持的最小目标集合。明确哪些质量/摩擦/六维力矩分量没有必要或无法从当前输入区分；不要求神经网络强行还原不可辨识参数。

## 3. C002信息边界与数据人口

本版质量、closer与friction逐门固定；B05几何及真实抓握状态会改变接触、杠杆、滑移和视觉。请分析多因素产生相似观测的情况，区分关门主动负载、耗散、惯性、grasp质量与机器人自身控制误差。不能用“轻/重”或单个closer标签替代实际动力学。

当前Teacher有质量/几何/门角真值，Student没有；关节PD动作不是力矩传感，sim applied effort、指部net contact force、针对door_handle的filtered contact，以及全身外部wrench不是同一种信号。给出需要哪些、哪些可省略、哪些部署来源尚未知。

数据必须包含接触前、短历史、无有效握持、单指/失抓、成功与失败episode。说明这些阶段哪些目标根本没有信息或没有定义，怎样保留缺失/不足信息样本的任务意义。允许按条件预测，但不能只筛长成功接触窗口后声称完整C002可辨识。

v27 shadow只证明旧模拟数据中的有限离线信息量：它排除了51个不足64-control-step窗口的episode，使用sim contact/effort估计，未回馈actor。请独立决定C002怎样重新收集/分组/切分/评估，避免同一door/episode或相同policy轨迹在train/test泄漏；同时区分window-weighted与episode-weighted结论。不要复跑旧shadow再把数字当新域结论。

## 4. 有明确目标以后再比较结构

按增量复杂度比较：当前LSTM但改善数据/任务暴露；给同一LSTM加辅助监督；独立历史编码器；需要时才融合视觉或增加更复杂预测模型。给同输入、历史和交互预算的公平对照，不让新增真值或更多数据替网络赢。

独立阅读UniFP和SixthSense的一手论文/作者资料，核对目标是当前估计还是未来预测、是否动作条件、输入/监督/机器人形态/接触假设、延迟与部署条件、所报结果能支持什么。借鉴数据构造、目标、时序建模或不确定性等部分即可，不能因为论文用了fusion/flow-matching就预选这些结构。允许简单回归/分类或纯recurrent policy足够；若选择复杂方案，要说明简单方案在哪里失败以及收益与代价。

两篇论文的事实入口在 `PAPER_REFERENCE_NOTES.md`；本地阅读提示仅是起点，Pro应独立继续核实和寻找相关原始研究。不要声称humanoid全身力估计已经适用于四足＋机械臂、持续抓柄、门约束和步态接触。

## 5. Student从开始就进入设计

分别描述Teacher的信息上限、Student部署输入、sim训练label及评估oracle。讨论模块共享、先训练再冻结、独立适配、联合训练、辅助监督/蒸馏或Student闭环finetune，并选推荐主线。

现有DAgger默认Teacher执行比例1.0，支持Student动作执行和在线Teacher查询，但没有自动课程/跨batch数据聚合；Student和Teacher记忆各自维护。你的方案必须写清Student自己的轨迹如何产生、何时得到目标或动作监督、历史/hidden怎样随真实轨迹演化、reset/snapshot/burn-in怎样一致。Teacher轨迹上的可辨识性或预测精度不能直接外推Student。

如果module训练使用Teacher特权输入，说明如何避免Student阶段接口/条件分布改变；如果只使用部署输入训练，说明Teacher是否也采用同等接口作为可比较的参照。若估计输出只被auxiliary loss使用，也要解释最终动作怎样受益、如何检验policy有没有使用新增信息。

与N01的接口只定义所需事件/状态/数据共享和职责边界：N01恢复图尚未被选型或实施，不可作为N02已经可调用的组件。两项可以共享轨迹，但能力与收益须分别报告。

## 6. 持续感知怎样转成期望的开门行为

请直接回答：N02能否实现Owner的行为目标；仅靠估计器是否足够；还需哪些最小行为/奖励/训练修改。

将“近中性姿态、arm主导、有条件roll/pitch、可控甩门、quiet握持、强回弹时持续握持”变成可判断的物理行为定义与决策过程。区分base平移、姿态调整和arm活动；不要把arm主导误写成base完全不动，也不要把出现roll/pitch本身当作有效适应。

困难程度不能由某个策略失败来循环定义；轻门可以有强closer，重门也可以没有主动回关。强阻尼、强回弹请求与快速闭合不等价；释放时门角、门速、惯性、摩擦、grasp可控性、机器人位置/姿态与通行余量共同影响后续。请说明为何一个信号足以或不足以决定hold/controlled-swing。

“强回弹一直握着”需讨论成立的物理区间与退出条件：身体仍在门扇影响区、arm可达且握持可靠时怎样维持；完全通过以后如何正常释放；若arm行程不足、抓握已丢失或信息不足，怎样调整base/重抓/停止本次动作。不能用永不松手解决所有情况，也不能把root_x>0等同全身已安全通过。

给出一个可实现的闭环流程/图及伪代码：获取观察→更新历史/目标估计→判断当前动作后果→调整base/arm/握持/释放→观察真实响应→重新决策。若不需要显式模式开关，请用同样清楚的方式说明连续policy如何学到与评估这些选择，避免仅展示一个标签分类器。

## 7. 最小实施与证据链

先提出最小端到端功能路径，选少量能区分信息不足、训练不足与行为不足的pilot。评估同时覆盖估计本身和在线控制：有/无信息对照、同等reward/行为支持的baseline、动作是否确实因有效反馈而改变、目标预测错误时的后果、自然成功与失败、姿态代价、释放后的门动态和trunk接触/净空。

保持完整C002+B05、同等部署输入和合理对齐的训练交互预算；不要把新门域、更强Teacher、改reward和换网络一起改后统称N02效果。可比较信息上限或短时oracle，但必须标明只是诊断。Student单独闭环完成任务，不能靠Teacher接管制造效果。

建议few-stage顺序路线和何时继续/换目标/不用独立估计器；工程初值要可解释，不把云端猜测变成一串严格本地门槛。不安排大规模矩阵，不现在启动训练或写防御/回归测试。Pro交付的完整方案应可落到当前source接口，但仍是建议，实施及预算由后续任务决定。

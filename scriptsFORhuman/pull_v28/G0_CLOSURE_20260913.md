# Pull v28 G0 closure — 2026-09-13 m5

**Status: BLOCKED_G0_INFRA_REPAIR_LIMIT.** The baseline implementation is applied, but G0 is incomplete. Do not start PA or label opening as failed.

P2 was first archived in local commit `9246460`: all 18 missing natural exact64 lanes completed, three milestone reductions retained. Ready and clean release were zero throughout. New source and assets were applied only afterward.

## Completed bounded evidence

| Gate | Result | Practical limit |
|---|---|---|
| PG1 | PASS | Direct source-input byte comparison; actual 28 bodies/20 joints and named contact mapping |
| PG2 | PASS | New reset/observation anchors and upper/lower D17 clamp at production override boundary |
| PG3 | Scoped PASS | Fixed wrist table, tower raw contact and persistent-release mapping; no release event occurred |
| PG4 | Runtime defect after CPU compose | Robot physical/config leaf deltas were correct, but flat config lost the dynamic env.robot reference, leaving actor input dimension0 |
| PG5 | PASS | LEFT 180° target relationship at same pose, RIGHT authored offset unchanged |
| PG6 | INCOMPLETE | 50-step zero high-level command contact passed, but PPO failed at initialization |
| PG7 | PASS | m5 old/MERGED matched default/hold/stage2 D37 comparison; all tracking gates, zero falls and slope passed |
| PG8 | NOT VALIDATED END TO END | Pipeline, watcher and reducer implemented; no full G0 checkpoint or natural learned-policy output exists |

The contact probe executes the existing frozen A2_Base leg policy under zero high-level pull command. Monitored trunk, arm_body0..6 and tower forces were 0 N over the first 50 steps. Release/E6 did not occur; matching an empty-event truth table does not establish late-stage behavior. Camera projection remains nominal, not optical/depth/occlusion qualification. X24 stochastic calibration remains open.

## Actual failed attempts and bounded repairs

1. contact_a1: process exit0 but no probe report. SimulationApp.close in finally terminated the framework before pending traceback/return propagation. Repair1 writes result/error before explicit exit; original hidden exception cannot be recovered.
2. contact_a2: explicit pre-policy construction error `Pull-v6 staged reset requires enable_staged_reset=true.` Repair2 follows the existing natural evaluator: registration enabled with `[1,0,0,0,0,0]`, banks disabled. Training reset ratios unchanged.
3. contact_a3: all scoped contact/mapping/anchor gates passed. No further contact run.
4. PPO G0 attempt1: intended scratch `checkpoint=null/full`, auto-load=false, 256 environments and 5 batches. `RecurrentActor` LSTM initialization fails at CUDA/cuDNN `_cudnn_rnn_flatten_weight` with `CUDNN_STATUS_BAD_PARAM`, before learning iteration1. Child exits0 through existing application teardown, but the existing runner correctly returns1 because `model_step_000005.pt` is missing. Peak GPU memory5240MiB, minimum headroom19336MiB; no evidence of OOM.

No third G0 repair or fallback was attempted. The plan's two-repair-per-cell limit is treated across this G0 cell, not reset for each subprobe. Owner decision is required to authorize a bounded additional diagnosis/fix, or leave this branch stopped. Any proposed event/reward/asset/A2_Base or budget change remains separately gated. Read-only diagnosis identified the direct root cause: flat materialization broke `env.config.robot: ${robot}`. Preprocessing updates root133/138 while the env robot retains0/0; the actual log shows `LSTM(0,256)`. A one-line reference-restoration patch is prepared in `OWNER_PROPOSED_FIX_NOT_APPLIED.patch` and deliberately not applied. See `OWNER_REPAIR_REQUEST_20260913.md` for the concrete additional repair request.

## Budget and terminal scope

P2: 18/18 missing evaluations completed; no old retraining. G0: six matched walk runs plus three contact attempts, **0 PPO batches completed** of planned5 (ceiling32). PA_S1/2/3: **NOT_RUN**, 0/18000 batches; warm NOT_RUN, no18500 ceiling breach. No high-level G0 natural evaluation could run without its full checkpoint.

The original three-seed endpoint denominator is preserved: `k=null, denominator=3, valid endpoints=0`. This is **NOT_ASSESSED_G0_INCOMPLETE**, not 0/3 or PULL_V28_OPENING_NOT_ESTABLISHED. PA history, margin and camera capability are unobserved; old P2 is diagnostic only. P3–P5 and Teacher/hardware were not started.

## Evidence

- `G0_DECISION_20260913.json` and `evidence/g0/` contain scoped probe, six walk metrics/comparisons, process receipts, exact PPO config and raw initialization traceback.
- `evidence/S1_S8_APPLICATION_RECEIPT.json`, `mainline_reference/20260913/SHARED_INPUTS_MANIFEST.json` and receipt retain input/application provenance. Extra PG7 replay/commands/postures and D435 nominal parameters have separate direct-content receipts.
- The actual asset is MERGED 28/20, rig140mm/38.76°, reset[0,.10,-.10,0,-.415,1.57], D17, wrist/tower/persistent-release bundle. Existing pull events/ready/Stage4, no K, A2_Base and physical gates were preserved.
- Raw outputs stay under canonical v28 logs roots backed by the user-owned SSD. No old raw evidence was moved or overwritten.

# Pull-v7 P2 diagnostic closure

The frozen P2 evaluator completed the missing natural exact64 lanes for `T_S1`, `T_S2`, and `C_S2` at steps 9500, 10000, and 10500. This closes only the old-recipe diagnostic readout. It does not route any result into pull v28.

## Runtime evidence

All three new v1.4 eval receipts completed with exit code 0:

- `pull_v7_p2_eval_t_s1_20260913`: 2026-09-13T14:28:20Z
- `pull_v7_p2_eval_t_s2_20260913`: 2026-09-13T14:29:25Z
- `pull_v7_p2_eval_c_s2_20260913`: 2026-09-13T14:28:24Z

Each milestone has a single streaming three-cell reducer output:

- `P2_MEDIATOR_step9500.json` and `.md`
- `P2_MEDIATOR_step10000.json` and `.md`
- `P2_MEDIATOR_step10500.json` and `.md`

The training runs were not restarted, `C_S1` was not evaluated, and no D2 scale retry, new asset, D17, or v28 bundle was applied.

## Diagnostic readout

At 10500, all T traces contain `a2_pull_v7_arm_target_overshoot_penalty`; both C traces omit it. The runtime D2 raw medians equal the recomputed stage1-plus target-overshoot medians in the reducer: T_S1 left/right `3.7500/3.7908`; T_S2 `2.0872/2.0300`. C_S2's corresponding recomputed medians are `5.7800/9.0056` without a D2 reward trace entry.

K5 is 64/64 for every side and cell. E5 is T_S1 left/right `63/64` and `64/64`, T_S2 `64/64` and `64/64`, and C_S2 `64/64` and `64/64`. No side in any cell has a ready window or clean-release episode. The 10500 median of episode-max release margin is T_S1 left/right `0.000227/0.000152`, T_S2 `0.010724/0.008858`, and C_S2 `0.000133/0.000028`.

These results retain K5/E5 behavior while exposing lower commanded-target overshoot in T. They do not demonstrate a ready window, clean release, opening closure, Teacher suitability, hardware suitability, or a basis to alter the pull-v28 baseline.

## Harness repairs recorded with this closure

`eval_agent_trl.py` now uses the explicit `eval_output_dir` as the evaluation `experiment_dir`, so P2 eval artifacts do not write into checkpoint/training directories. `eval_p2_cell.sh` traces the D2 reward only for T cells; C cells retain the original trace list. `analyze_p2.py` now accepts the canonical relative eval root and its step10000/10500 symlink targets without resolving them against the repository root.

Main finalized only the three completed legacy train receipts using their original process/checkpoint finalizer. Legacy PASS remains process-only and is explicitly annotated. Only their legacy events and the three new eval completion events were handled. C_S1 remains DECLARED.

T_S2/C_S2 share seed lineage; T_S1 has no matched C_S1 control, so cross-cell differences are descriptive. All 18 lanes have 64 natural births and 64 traced episodes. Ready, clean release, high-margin B steps, E6 and E7 are zero throughout. This does not establish geometric impossibility.

Raw traces remain at canonical logs_eval paths. Only not-started step10000/10500 parents were routed to user-owned SSD storage because expected trace volume exceeded root free space; no old output was moved. See evidence/p2/P2_STORAGE.json.

Decision: old D2 recipe archived; continue to pull-specific G0, without another D2 run or C_S1. New shared source/assets were not active during P2.

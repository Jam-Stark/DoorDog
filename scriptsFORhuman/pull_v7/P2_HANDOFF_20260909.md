# Pull v7 P2 handoff：训练已完成，里程碑评估与收尾待做

交接时间：2026-09-09 15:20 HKT。工作目录 `/home/baoquanc/workspace/DoorDog-A2_Piper_pull_v0`，分支 `codex/a2-piper-pull-v0-20260803`，HEAD `025ce28`（工作区含未提交改动，见 §2）。

你是接手 P2 后半段的执行团队。Main planner 保留 scope、acceptance、Git、memory 文件、停止与最终整合权；Owner 已批准的预算与处理变量不可更改。以下内容全部来自磁盘与进程实况核对，不是转述前一个团队的口头汇报（该团队在回传报告前被终止）。

## 1. 先读什么

1. `AGENTS.md` → `.ai/ROLE.md`、`.ai/PROJECT.md`、`.ai/WORKFLOW.md`、`.ai/LONG_RUNNING_TASKS.md`、`.ai/TEAM_STATE.md`；Codex runtime 再读 `.codex/AGENTS.md`，需要委托时读 `.codex/TEAM.md`。
2. 方案：`scriptsFORhuman/pull_task/a2_piper_pull_v7_stage_plan_20260909.md`，重点 §1（阻塞判断）、§4.4（中介与判据）、§4.5（预登记分支）。
3. 冻结执行合同：`scriptsFORhuman/pull_v7/P2_CONTRACT.md`（格、命令、eval 放置规则、门限；§2.1 说明为什么一次跑到 10500）。
4. memory 路由：`memory/a2-piper/pull-lr-full-stage/description.md` 与 `TODO.md`（只读；memory 由 Main 写）。
5. 需要理解中介定义时再读 `scriptsFORhuman/pull_v7/P0_DIAGNOSIS.md` §2–§3；不要重读旧 session 或 P1 全文。

## 2. 已完成（磁盘实况）

### 2.1 代码与配置（未提交）

| 路径 | 内容 |
|---|---|
| `gr00t/rl/envs/door/door_open_a2_pull.py` | 仅新增 `_reward_a2_pull_v7_arm_target_overshoot_penalty`（+62 行，位于 `_reward_a2_pull_v6_*` 之后）：`q_target = arm_default + action_scale·_delta_actions`，`raw = Σ_j max(0,q−u)+max(0,l−q)`，硬限位取 `simulator.hard_dof_pos_limits[_a2_arm_dof_indices]`；形状/索引不符直接 raise。未触碰任何共享类。 |
| `gr00t/rl/config/ablation/wbmanip/pull_v7_p2_T_S1.yaml`、`pull_v7_p2_T_S2.yaml` | 继承 `pull_v26_8_backbone_P_S{1,2}`，只加 `rewards.reward_scales.a2_pull_v7_arm_target_overshoot_penalty: -1.0` |
| `scriptsFORhuman/pull_v7/run_p2_cell.sh` | 唯一训练入口 `GPU CELL OUTPUT`；resolve → `verify_p2_config.py` → `pull_v26_8/runner.py --required model_step_010500.pt`；只接受 GPU1/2/3 |
| `scriptsFORhuman/pull_v7/verify_p2_config.py` | T 的 reward_scales = 9000 源 + 且仅 + 该项（90→91 项）；C 逐项相同；checkpoint/full/10500/1024/seed/133-138/ratios/bank 断言 |
| `scriptsFORhuman/pull_v7/verify_p2_overshoot.py` → `P2_OVERSHOOT_REFERENCE.json` | 用 P0 CSV 的六关节 target 离线重算 overshoot：P_S1 LEFT j3 中位 3.75、j5 中位 0/p90 1.0/max 2.03 |
| `scriptsFORhuman/pull_v7/eval_p2_cell.sh` | `GPU CELL STEP TRAIN_ROOT EVAL_ROOT`，每侧 exact64 natural，override 与 `pull_v26_8/continue_eval_cell.sh` 相同，仅改 cell/step/GPU；**尚未在真实 checkpoint 上执行过** |
| `scriptsFORhuman/pull_v7/analyze_p2.py` | P2 中介 reducer：`--eval-root --step {9500,10000,10500} --cells ... [--output] [--report-only]`，复用 `analyze_p0.py`；**尚未在真实 eval 输出上执行过** |
| `scriptsFORhuman/pull_v7/watch_p2.py` | 里程碑 watcher；**从未启动**（无 tmux、无 `watch_state.json`） |
| `scriptsFORhuman/pull_v7/P2_CONTRACT.md` | 冻结合同，含 NO_DELEGATION_REASON（其 runtime 无 grok slug） |

### 2.2 Smoke（已做，不重复）

`logs_rl/a2_piper_pull_v7/p2_20260909_smoke/{T_S1,C_S2}`，256 env、5 新 batch、上限 9005，child/wrapper 均 0，峰值 6.1/6.3 GB。T_S1 的 D2 项 `Mean episode rew_a2_pull_v7_arm_target_overshoot_penalty` 从 0.0000（batch 1）升到 −0.3252（batch 5）；C_S2 日志中该项不存在。真实约束：full 续训要求 `num_total_batches` 大于已加载 global step，第一次 `=5` 被 `RuntimeError` 拒绝，日志保留在 `*_attempt1_batch_ceiling_rejected/`。

### 2.3 三格训练：全部跑完 9001→10500

| cell | GPU | 启动 | 结束(HKT) | child/wrapper | 峰值 MiB | checkpoints |
|---|---|---|---|---|---|---|
| T_S1 | 1 | 01:47 | 11:18 | 0/0 | 16456 | 9250/9500/9750/10000/10250/10500 |
| T_S2 | 2 | 01:47 | 11:22 | 0/0 | 16530 | 同上 |
| C_S2 | 3 | 01:47 | 11:19 | 0/0 | 16487 | 同上 |

输出 `logs_rl/a2_piper_pull_v7/p2_20260909/<cell>/`（`resolved_config.yaml`、`config_check.json`、`isaac.log`、`runtime_result.json`、`gpu_memory.csv`）；启动日志 `<cell>_launcher.log`。每格 1500 新 batches、98,304,000 transitions，均在授权内。三格 `isaac.log` 均含 `Loaded checkpoint from step 9000`，reward 表 1500 行。

receipt：`.ai/runtime/runs/pull_v7_p2_{t_s1,t_s2,c_s2}_20260909/RUN_RECEIPT.json` 仍是 `state: RUNNING`（未 finalize），旁有 `exit_code.txt=0`、`finished_at.txt`。当前无任何 P2 tmux 会话，GPU1/2/3 无进程。

### 2.4 训练日志代理读数（仅供定向，不是 natural 证据、不是门限依据）

`Mean episode rew_*`（每 episode 累计、乘 dt 后、训练混合 reset 分布）：

| cell | 项 | batch 10 | 500 | 1000 | 1500 |
|---|---|---:|---:|---:|---:|
| T_S2 | D2 overshoot | −1.84 | −1.13 | −0.68 | −0.55 |
| T_S2 | limits_dof_pos | −0.270 | −0.263 | −0.258 | −0.212 |
| T_S2 | pull_door_hinge | 1.14 | 0.95 | 0.82 | 1.02 |
| T_S1 | D2 overshoot | −1.28 | −1.19 | −1.13 | −1.13 |
| T_S1 | limits_dof_pos | −0.178 | −0.173 | −0.176 | −0.164 |
| T_S1 | pull_door_hinge | 1.20 | 1.25 | 1.20 | 1.02 |
| C_S2 | limits_dof_pos | −0.274 | −0.286 | −0.262 | −0.295 |
| C_S2 | pull_door_hinge | 1.09 | 1.18 | 1.07 | 1.15 |

可以说的：T_S2 的 overshoot 在 1500 batch 内下降约 70%，limits_dof_pos 同向下降；T_S1 只降约 12%，两个来源对 D2 的响应不同。不能说的：任何关于 margin≥0.07、ready、保留能力的结论——那些只能来自 natural eval 与 `analyze_p2.py`。

## 3. 未完成（你的任务，按顺序）

### 3.1 资源与 ledger

- `nvidia-smi`：GPU0 是其他用户的 LightNav 服务（约 14 GB），永不使用；GPU1/2/3 当前空闲，启动前再核对。
- `python .ai/scripts/team_state.py status` 当前 `ACTIVE adaptive`。核对前团队是否真的登记了 task `pull_v7_p2_20260909` 与 lease（`gpu:1/2/3`、两个 output root、四个 path）；缺失则按 `.ai/TEAM_STATE.md` 登记你实际占用的资源，结束时释放。

### 3.2 启动 C_S1 补跑（Owner 已批准）

```bash
tmux new -d -s pull_v7_p2_c_s1 'bash scriptsFORhuman/pull_v7/run_p2_cell.sh 1 C_S1 /home/baoquanc/workspace/DoorDog-A2_Piper_pull_v0/logs_rl/a2_piper_pull_v7/p2_20260909/C_S1 > logs_rl/a2_piper_pull_v7/p2_20260909/C_S1_launcher.log 2>&1'
```

把 `.ai/runtime/runs/pull_v7_p2_c_s1_20260909/RUN_RECEIPT.json` 从 DECLARED 补成实际 gpu/launched_at/RUNNING（其余字段已填）。启动后核对 batch-1：`Loaded checkpoint from step 9000`、首个 update 9001、LEFT/RIGHT 512/512、reward 表**没有** D2 项。预计约 9.2 h（22 s/batch）。

### 3.3 三格里程碑 natural eval（先做，不等 C_S1）

每次调用评一格一个 step 的两侧（约 25 min，峰值约 3.8 GB）：

```bash
bash scriptsFORhuman/pull_v7/eval_p2_cell.sh <GPU> <CELL> <STEP> \
  /home/baoquanc/workspace/DoorDog-A2_Piper_pull_v0/logs_rl/a2_piper_pull_v7/p2_20260909 \
  /home/baoquanc/workspace/DoorDog-A2_Piper_pull_v0/logs_eval/a2_piper_pull_v7/p2_20260909
```

- 放置：C_S1 在 GPU1 训练后，eval 只用 GPU2/GPU3，每卡同时一个；不在训练卡旁边跑 eval。
- 顺序：先 9500 的 T_S1/T_S2/C_S2，跑完立刻 `analyze_p2.py --step 9500 --cells T_S1 T_S2 C_S2`，再 10000，再 10500。共 9 次调用、18 侧。
- 每次先 diff 一遍 `eval_p2_cell.sh` 与 `scriptsFORhuman/pull_v26_8/continue_eval_cell.sh` 的 override 段，确认只有 cell/step/GPU 不同；`.hydra/runtime_config.yaml`、出生 trace、exact64 硬检查沿用 `pull_v26_8/verify.py` 与 reducer 合同。
- 每侧一次，不重跑；eval 失败（非 0 退出、缺 artifact、`PULL_V26_8_INVALID`）保留日志并报告，不改 override 重试。

### 3.4 reducer

`analyze_p2.py` 未在真实输出上运行过。首次运行时核对：margin 重算与 trace `workspace` 原值误差 ≤1e−7（P0 口径）、ready 重算不一致数为 0、每格每侧分母 64、D2 项 `reward_raw` 与按 target 离线重算一致。只修 reducer 自身的真实缺陷（WRITE_SET 内），不改 P0/P1 文件与训练语义。产物 `P2_MEDIATOR_step<step>.json/.md` 落在 eval 根。

### 3.5 C_S1 到达 9500/10000/10500 后

同 §3.3 评估，并用四格重跑对应 step 的 `analyze_p2.py`，覆盖之前三格版本（保留旧文件为 `*_3cells.*`）。

### 3.6 收尾

- 按 `.ai/LONG_RUNNING_TASKS.md` 状态机把四份 receipt 推到 `PROCESS_EXITED → CHECKPOINT_VALIDATED → EVAL_RUNNING → PASS|FAIL|INCONCLUSIVE`（这里的 PASS 只指 run/eval 完整，不指策略结论）。
- 释放 lease；不 deactivate ledger（由 Main 归档）。
- 写 `scriptsFORhuman/pull_v7/P2_RESULTS.md`（见 §5）。

### 3.7 资产迁移门 A（Owner 2026-09-09 15:11 新增；与 §3.3 并行的独立 lane）

要求：v7 阶段把 robot 模型换成 `gr00t/rl/data/robots/a2_piper_vpiper_final_20260906`（主入口 `a2_piper.urdf`；USD `a2_piper.usd` + `configuration/{a2_piper_base,physics,robot,sensor}.usd` 相对引用层）。规则见方案 §4.6；先读资产包 `README.md`、`docs/LOCAL_VERIFY.md`、`validation/usd_import_readback.json`、`config/joint_mapping.csv`。

已核对事实（不要重复做）：20 个关节名/序/限位与 `gr00t/rl/config/robot/A2_Piper/a2_piper.yaml` 逐项相同；link 集合 = 原 27 + `vpiper_main`/`vpiper_support`/`metal_plate_5mm`（30 rigid bodies，+0.754 kg，51 凸碰撞块 + 板碰撞箱）；`arm_j0` 原点 Z 0.154→0.147431554755 m；USD 内无绝对路径；资产包只做过 Isaac 导入读回，没有仿真步进或策略评估。`a2_piper.yaml:174-176` 通过 `asset.asset_root + usd_file` 解析（`gr00t/rl/simulator/isaacsim/isaacsim.py:1190-1200`）。

**硬规则：P2 的 T/C 里程碑 eval（§3.3、§3.5）一律用旧资产**——那是 D2 的因果读数。门 A 的评估另放新目录，不与 P2 读数混表。

任务：

1. **robot config 变体**：新建 `gr00t/rl/config/robot/A2_Piper/a2_piper_vpiper_20260906.yaml`（或 Hydra 同层等价写法），只改 `asset.urdf_file/usd_file` 指向新目录，并把三个新 body 按实际 USD body 集合补进 `body_names` 与 `penalize_contacts_on`（安装件撞门框应计为 undesired contact；`randomize_link_body_names` 仍只 `trunk`）。先 trace `body_names` 在 `legged_robot_base`/`isaacsim.py` 中如何建立 body 索引、`num_bodies` 有无断言、`_get_a2_pull_whole_body_clear_mask`（`door_open_a2_pull.py:5456-5473`）是否遍历全部 body；顺序不一致必须 raise，不能靠默认。再新建一个 pull_v7 ablation 选择器 `pull_v7_asset_<cell>` 仅切换 robot 变体。
2. **USD 层 diff**：用 `pxr`/`usdcat` 把旧 `A2_Piper/a2_piper.usd`（根层含 `arm_j2` drive 属性覆盖）与新根层、`configuration/a2_piper_physics.usd` 的 drive/collision/articulation 属性逐项 diff；对每个旧有而新缺的覆盖，核对 `gr00t/rl/simulator/isaacsim_articulation_cfg.py` 是否在 spawn 时统一设置（如是则无需补；如否则报告，不擅自改资产包）。
3. **构造 smoke（一次）**：64 env、旧/新资产各一次 `--cfg job --resolve` + 短 eval 构造（可复用 G1 口径 `max_episode_length_s=0.02`、`enable_staged_reset=true`、ratios `[1.0,0,0,0,0,0]`、bank false）；比较 dof 名序/限位/默认角/body 名序/数量、FrameTransformer 目标帧读回、LEFT 镜像相对旋转 180±0.05°（`scriptsFORhuman/pull_v26_8/g1_reduce.py` 口径）；把差异表落到 `logs_eval/a2_piper_pull_v7/assetA_20260909/construction_diff.json`。任何维度/顺序不一致即停止报告。
4. **迁移评估**：在新资产上对 5 个 checkpoint 各做一次 natural64/侧（复用 `eval_p2_cell.sh` 逻辑但换 robot 选择器与输出根 `logs_eval/a2_piper_pull_v7/assetA_20260909/`）：T_S1/T_S2/C_S2 的 `model_step_010500.pt` 与两源 `wave2/train/P_S{1,2}/model_step_009000.pt`。用 `analyze_p2.py`（加 `--eval-root` 指向新目录）出 K5/D/E4/E5、4A/4B 保留指标、E5 后 margin/overshoot 中介，并列出与同 checkpoint 旧资产读数的差。只报告，不裁决。
5. GPU：与 §3.3 共享 GPU2/GPU3；顺序上 P2 的 9500 eval 优先，门 A 的 smoke 可先做（约 3.8 GB，几分钟），迁移评估排在 P2 三格 10500 eval 之后。

门 A 之后的任何新训练（包括可能的再适应续训）都要 Owner 另批预算；本 handoff 不授权在新资产上训练。

## 4. 边界

- WRITE_SET：`scriptsFORhuman/pull_v7/` 下 P2 文件（`analyze_p2.py`、`watch_p2.py`、`eval_p2_cell.sh` 的真实缺陷修复；新增 `P2_RESULTS.md`、门 A 的 `eval_assetA_cell.sh`/`construction_diff.py`/`ASSET_A_REPORT.md`）、新建 `gr00t/rl/config/robot/A2_Piper/a2_piper_vpiper_20260906.yaml` 与 `gr00t/rl/config/ablation/wbmanip/pull_v7_asset_*.yaml`、`logs_rl/a2_piper_pull_v7/`、`logs_eval/a2_piper_pull_v7/`、`.ai/runtime/`。
- 不改：`door_open_a2_pull.py` 的 D2 方法、两个 T 配置、`run_p2_cell.sh`、现有 `a2_piper.yaml`、资产包 `a2_piper_vpiper_final_20260906/` 内任何文件、任何 `pull_v26_8` 脚本、P0/P1 文件、`memory/`、方案文档、`.codex/`、`Codex-Cashier/`、任何共享 env 类。
- 不重跑训练、不做新 smoke、不延长 batch、不动 scale、不启动 D1/结构 clamp、不做 Teacher/handoff/hardware。
- 不 `git add/commit/push`。不用 fallback、broad catch、默认值或假数据；无效状态直接 raise 并报告。
- 判定权：reducer 只报告。9500 门已不再用于停训（训练已结束），但它的读数决定方案 §4.5 走哪一行；该决定由 Main/Owner 做，你给读数。

## 5. 交付格式（`P2_RESULTS.md` 与最终回复）

```
Result   — 四格状态；18(+6) 侧 eval 完成数；reducer 产物路径；门 A 完成到哪一步
Table    — 每 cell × side × step：K5/D/E4/E5、handoff_reached/tangent≥0.6/handle_crossed、
           overshoot(per-joint 中位/p90)、margin 每 episode max(中位/p90)、有余量 B 步、
           j3/j5 首次离限位 age、同一步通过条件数分布、ready/clean 计数、integrity
Gate     — 逐条对照方案 §4.4/§4.5：哪些读数满足/不满足；只陈述，不裁决
Asset A  — 构造 diff 表（dof/body/帧/镜像）、USD 层 diff 结论、5 个 checkpoint 新旧资产
           natural64 对照表（ASSET_A_REPORT.md）
Runtime  — C_S1 启动/结束、eval 每侧退出码与峰值显存、receipt 状态、lease 释放
Not run  — 未做与原因；发现的脚本缺陷及修复 diff 摘要
```

数字给精确值，不给形容词。

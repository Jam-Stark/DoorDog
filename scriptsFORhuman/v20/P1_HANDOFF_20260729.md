# base_v20 P1 handoff — 2026-07-29

## Status

`P1 BLOCKED`: do not launch base_v20 formal training. No completed probe run has
produced an `ARC_PROBE_REACHED` episode, and the interrupted R23 run produced no
strict-valid summary or trace artifact. This snapshot is an investigation
handoff, not a training-ready or P1-PASS candidate.

## Evidence boundary

- The P1 gate remains `theta_send >= 0.90 rad` with at least `46/48`
  strict-valid feasible pooled episodes in F0 or F1.
- R14 reached about `2.618 rad` hinge motion in three smoke environments but
  failed joint/root validity, so the evidence does not prove geometric
  infeasibility.
- R21 ended `4/4 JOINT_LIMIT` before formal capture.
- R22 ended with two `JOINT_LIMIT`, one `ARC_PROBE_OVERSPEED`, and one
  `ARC_PROBE_ROOT_BOUND`; capture bookkeeping included pre-capture root motion.
- R23 attempted activation-time canonical capture but was interrupted before
  producing `a2_hold_oracle_summary.json` or `stage2_5_step_trace.json`.

## Highest-priority bounded repair

Make arc-reference advancement transactional. Compute a DLS proposal without
mutating the persistent hinge reference; commit the next reference only for an
arm command that is actually applied. Freeze the reference during F1 base
relief, synchronize the held arm target to physical joint state, and resume arc
advancement only after relief restores a legal arm proposal. Keep the
preregistered F1 limits at `0.10 m` translation and `0.15 rad` yaw.

After the repair, allow exactly one F1/0.90-rad/seed0/4-env smoke. Proceed to
pooled48 only if all four episodes have canonical capture and reach the target
without joint-limit, IK/action, overspeed, root-bound, crossing, collision, or
contact failures. Otherwise close P1 as a geometry/control blocker without
further iterative control revisions.

## Package contents

The top-level handoff ZIP contains this note, the current P1 runner and tests,
the pre-R23 `.orig` investigation snapshots, and a SHA-256 manifest. Repository
paths are preserved inside the archive.

## Approved bounded amendment — P1_TX1_TRANSACTIONAL_REFERENCE

`P1_TX1_TRANSACTIONAL_REFERENCE` is approved for exactly one bounded runtime
attempt. It is not a P1 PASS prediction. The scientific thresholds and
selection matrix remain unchanged.

The candidate must first pass the static admission suite. It may then run one
and only one `F1 / 0.90 rad / seed0 / 4-env` smoke on a single idle physical GPU
in 0–6. Any 1/4 failure closes P1 as `P1_PHYSICAL_BLOCKER`, with no second code
revision, parameter tuning, or G1–G7 launch. Only a 4/4 strict-valid smoke may
advance to the fixed F1/0.90 pooled48 gate.

Physical GPU7 is unavailable and must not be used by P1, formal training,
evaluation, or rendering. Future formal mapping, if P1 and P2 eventually pass,
is G1→GPU0 through G7→GPU6; evaluation waits for a training GPU to be released
or for all seven training groups to finish.

Current status remains:

```text
P1_TX1 approved for one bounded runtime attempt.
P1 remains BLOCKED and NOT PASS.
FORMAL TRAINING READY = false
G1-G7 MAY LAUNCH = no
LEGAL PHYSICAL GPUS = 0-6 only
```

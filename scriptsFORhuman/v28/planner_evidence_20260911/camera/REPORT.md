# v28 CAMERA-RETHINK lane report（base_v28；只读调查）

日期：2026-09-11 HKT。范围：CPU 几何，无 Isaac、无渲染、无 GPU、无硬件。所有输出在 `/tmp/v28_team2/camera/`，未改动任何 git worktree。

证据等级：**INSPECTED** = 从 source/config/URDF 直接读出；**COMPUTED** = 本 lane 在 URDF mesh / 归档 v27 trace 上做的 CPU 计算；**NOMINAL** = 设计标称值，未测量。所有“可见”都是针孔 + Min-Z 的**几何上界**，不含门板/墙遮挡与 stereo 有效性，不是成像证据。

脚本：`w1_urdf_width.py`（URDF y 范围）、`w1b_footprint.py`（xy 凸包）、`w1c_hip_sweep.py`（髋角扫掠）、`w2_crossing.py`（穿门间隙）、`w3_layouts.py` + `w3b_tables.py`（布局覆盖，复用 `scriptsFORhuman/v28/v28_u3f0_geometry_precheck.py`）、`w4_tower_height_occl.py`（低塔架 mesh 射线遮挡，复用安装包 `pkgcode`）。输出表在 `out/`。

**自校验（COMPUTED）**：(1) `w3` 重建的 `base_left/base_right/wrist` 光学位姿与冻结的 `planner_evidence_20260909/camera/variants/U3_F45_B15.json` 逐项一致（|Δp| = 0.000 mm，Δrot = 0.0000°）；(2) `w4` 在 θ45/180 mm 上复现 planner_evidence camera `REPORT.md:11-19` 的全部数字（bar visible RGB/depth 43/52 @o=0，29/29 @o=0.020，24/24 @o=0.035，上指尖 13%）。

---

## 0. 对 Owner 三个疑问的直接回答

1. **“板上双相机加宽机身、损害通过性”——几何上不成立。** 相机总成外缘在 trunk 系 y = ±0.200 m，而默认站姿下腿/足已经到 ±0.2348 m。把全部 `v28_base_*` 碰撞体从 URDF 里删掉，机器人的 xy 凸包**逐点不变**（全宽 0.4696 m）。在 128 条 v27 穿门 episode 上，带相机与不带相机的最小侧向间隙**每一行都到毫米相同**；因相机而碰撞或 <3 cm 的 episode 占比 = **0.0%**。真正吃掉余量的是穿门时的偏航角（§1.3）。
2. **“单前视 + 腕机是否够”——取决于要不要 Stage3–5 的环境通道，不取决于相机数量。** 单台**上仰 15°** 的中置相机保住了几乎全部把手侧门框与门板自由边通道；真正掉的是 LEFT 门的**铰链侧门框**（Stage3 depth 43%→9%，Stage4 20%→1%）和 base RGB 的臂遮挡冗余。把上仰角改回 0°、或搬到旧 head 位置、或干脆只留腕机，才会出现材料性损失（§2）。
3. **“需求应来自任务几何而非旧轨迹”——已做成两套独立评估。** §2.2 的 canonical posture set 完全由门几何与 v28 reset 姿态生成，与任何 Teacher 无关；§2.3 的 v27 replay 单独标注为**条件性**结论。两者在“上仰角比相机台数重要”“腕机在 Stage3–4 是 foveal 传感器”上一致；在“第二台 base 相机值不值”上只有 v27 replay 给得出量化差。

---

## 1. 通过性（Task 1，COMPUTED）

### 1.1 各部件侧向范围
资产 `gr00t/rl/data/robots/a2_piper_vpiper_final_20260906/a2_piper.urdf`，腿姿 `gr00t/rl/config/robot/A2_Piper/a2_piper.yaml:122-134`（hip 0 / thigh 0.5 / calf −1.0），臂姿 = v28 reset `[0,0.10,−0.10,0,−0.52,1.57]`。另两个候选资产的侧向极值与下表一致：`a2_piper_v28_cut_20260909/` 只把 `vpiper_main` 从 ±0.1166 收到 ±0.0990，`a2_piper_v28_merged_20260909/` 把三个 mount link 并入 trunk；两者的 trunk 碰撞仍到 ±0.2000、足仍到 ±0.2348，**通过性结论对三个候选资产都成立**。

| 元素 | 来源 | y 范围 (m) | 半宽 |
|---|---|---|---:|
| trunk 主碰撞盒 | `a2_piper.urdf:599` box 0.24×0.28×0.17 | ±0.1400 | 0.1400 |
| `metal_plate_5mm` 碰撞包络 | `:1361` box **0.25×0.1**×0.004 | ±0.0500 | 0.0500 |
| `vpiper_main` (convex meshes) | 15 块 | −0.1165…+0.1166 | 0.1166 |
| v28 base 横梁 | `:653` box 0.022×**0.33**×0.008 | ±0.1650 | 0.1650 |
| v28 base 相机鞍座 | `v28_base_*_saddle_collision` | ±0.1950 | 0.1950 |
| **v28 base 相机外壳** | `:755` 等，y 中心 ±0.155、长 0.09 | **±0.2000** | **0.2000** |
| 大腿 `*_thigh` | 3 块 | ±0.2279 | 0.2279 |
| 小腿 `*_calf` | 4 块 | ±0.2209 | 0.2209 |
| **足 `*_foot`** | sphere | **±0.2348** | **0.2348** |
| `wrist_camera_tower` | 支架+外壳盒 | −0.0488…+0.0450 | 0.0488 |

注：所谓“250×100 mm 金属板”的 250 mm 是**前后向**（x 0.018…0.268），侧向只有 100 mm；加宽机身的是横梁与外壳，不是板。

### 1.2 凸包与髋角敏感度
xy 凸包（`w1b_footprint.py`）：`with_cams` 与 `no_cams` 的 14 个顶点**完全相同**，x −0.4226…+0.3912、y ±0.2348、全宽 0.4696 m；`cams_only` 只有 0.400 m 宽且 x 仅 0.0097…0.0660，整块落在前后足连线之内。髋角统一外展/内收扫掠（`w1c_hip_sweep.py`，hip ∈ [−1.0, +0.3] rad）中腿的半宽始终 0.2191–0.3718 m，**从未低于相机的 0.2000 m**；外展每 +0.1 rad 增宽约 +0.048 m。

### 1.3 穿门时刻的侧向间隙
数据：`logs_eval/base_v27/v27_bilateral_hardening_20260905/wave_a/step3000/C_S21/nominal/{left,right}/stage2_5_step_trace.json`，首 episode，64/64 env 两侧都越过门平面。越门判据用环境自身的 `root_pos_rel.x > 0`（`gr00t/rl/envs/door/door_open_a2_base.py:2295`）。门模型按 `gr00t/rl/isaac_utils/playground/env_rand/door.py:355-379`（门框 collider 内缘在 |y| = W/2，x ∈ [−0.07, 0.03]）与面板半厚 0.02 重建；W 取拟合分布 p5/p50/p95 = 0.82/0.957/1.08（planner_evidence camera `REPORT.md:75`）。

穿门时刻读数（COMPUTED）：hinge p5/p50/p95 = LEFT 0.993/1.159/1.372、RIGHT 0.903/1.093/1.393 rad；root yaw p50 = LEFT **−0.218**、RIGHT **−0.345** rad。

| 门宽 W | 侧 | 窗口 |root_x|<0.6 m 内最小间隙 min/p5 (mm) | 碰撞占比 | <30 mm 占比 |
|---|---|---|---:|---:|
| 0.957 (p50) | left | **带相机 25/42 · 不带相机 25/42** | 0.0% / 0.0% | 3.1% / 3.1% |
| 0.957 (p50) | right | **带相机 −23/29 · 不带相机 −23/29** | 1.6% / 1.6% | 6.2% / 6.2% |
| 0.82 (p5) | left | 带 −44/−27 · 不带 −44/−27 | 23.4% / 23.4% | 62.5% / 62.5% |
| 0.82 (p5) | right | 带 −92/−40 · 不带 −92/−40 | 28.1% / 28.1% | 70.3% / 70.3% |
| 0.82 (p5) | left/right | **仅相机总成** 35/56 · 1/24 | **0.0%** | 0.0% / 7.8% |

**因板上相机而碰撞或 <3 cm 的 episode 份额 = 0.0%（两侧、三档门宽、穿门时刻与整个通过窗口都为 0）。** 相机与腿的差值恒为 0 mm。

原因（COMPUTED）：机器人在门平面上的**扫掠宽度**随偏航角急升，而短而宽的相机横梁随偏航角反而**变窄**——

| root yaw (rad) | 0 | 0.10 | 0.22 (LEFT p50) | 0.345 (RIGHT p50) | 0.42 |
|---|---:|---:|---:|---:|---:|
| 整机扫掠宽 (m) | 0.470 | 0.529 | **0.601** | **0.671** | 0.709 |
| 仅相机总成 (m) | 0.400 | 0.401 | 0.397 | 0.387 | 0.378 |

有效净开口 ≈ W·sin h + 0.02·cos h − 0.02：W=0.82 @ h=1.0472 → **0.700 m**；W=0.957 @ 1.0472 → 0.819 m。即在 p5 窄门 + 门刚过 60° 的最坏组合下，v27 Teacher 的斜身穿门（0.671 m）只剩 29 mm 余量，而相机贡献 0 mm。**passability 是 Teacher 行为问题（偏航 + 越门时机），不是相机问题。**

---

## 2. 备选布局覆盖（Task 2，COMPUTED）

评估的相机（全部用 SDK 实测 D435i 内参与 stream 间外参；head 另给 Student 实际的 384×136 超广角 RGB）：`base_left/right_B15`（±0.155，上仰 15°）、`base_centre_B15/B0`（[0.025, 0, 0.19]，上仰 15°/0°）、`head_d435i_B0/B15`（trunk 系 [0.3381, 0.0336, 0.0525]，父体 = trunk，见 `DepthADD_v3_Standard_Student_Export_Spec_20260825.yaml:117,120`）、`wrist_F45_180 / F39_140 / F345_120`。表里的份额是**逐姿态的 union**（该布局至少一台相机看到）。

### 2.1 不依赖轨迹的 canonical posture set（由任务几何生成）
接近族：距离 1.5/1.2/1.0/0.8 m × 侧移 ±0.15 × 偏航 ±17°，臂在 v28 reset 姿态；抓握族：在 PiPER 限位内均匀采样臂关节，用 FK 过滤 TCP 高度 0.85–0.95 m（世界）、离面板 x 0.25–0.60 m、法兰接近轴水平 ±20°、塔架朝上 ±50°，保留 400 组，再把把手放到该姿态的 TCP 上（hinge 0 与 0.5 rad，偏航 ±10°，n=1200/档）；通过族：机器人在门口 x −0.4/0/+0.4、偏航 ±20°，门 60°/90°。

接近族（1.0 m，LEFT/RIGHT 平均，depth）：**所有布局把手 100%**；把手侧门框 V0 100 / V1a 100 / V1b 100 / V2b 100 / V3 100；铰链侧门框 V0 67–78 / V1a 67 / V3 56。→ **Stage0/1 找门找把手，几台相机、什么位置几乎都够**，因为行走姿态下腕机光轴 −15° 本身就是一台前视相机。

抓握族（逐相机，LEFT，hinge 0，depth）：把手 `wrist_*` **100%**（三种塔高都是），`base_left_B15` 65%、`base_right_B15` 57%、`base_centre_B15` 23%、`base_centre_B0` 7%、`head_d435i_*` **0%**；把手侧门框 base_left 84 / base_centre_B15 83 / base_centre_B0 7 / head 0 / wrist 16；门板自由边 base_left 93 / base_centre_B15 93 / base_centre_B0 10 / head 0 / wrist 22。
→ **两个与相机台数无关的结论**：(a) 上仰 15° 是 base 相机能不能在抓握期看到门框/自由边的分水岭（83% vs 7%）；(b) 旧 head 位置（低 137 mm、前移 313 mm）在抓握期对门框/自由边**全为 0%**，被自身俯仰与距离挤出画面，不是可用的替代位。

通过族（LEFT，门 60°）：门框 ≤22%、门口地面 0%（所有布局），门板自由边 100%。→ **没有任何一个候选布局能在通过阶段看到门口地面**；base 相机下缘 −14°，地面 2.2 m 外才进画面（与 planner_evidence `REPORT.md:67` 一致）。

### 2.2 v27 轨迹 replay（**条件性**：取决于旧 Teacher 的姿态）
C_S2 / C_S21，双侧，66,688 帧；union、depth 通道，% ：

| lane/stage | 指标 | V0 双 | V1a 中置15° | V1b 中置0° | V2b head15° | V3 仅腕机 |
|---|---|---:|---:|---:|---:|---:|
| C_S2/left st3 | 把手侧门框 | 99 | 99 | 39 | 33 | 32 |
| C_S2/left st3 | 铰链侧门框 | 43 | 9 | 5 | 1 | 0 |
| C_S2/left st4 | 门板自由边 | 94 | 94 | 59 | 56 | 51 |
| C_S2/right st3 | 把手侧门框 | 100 | 100 | 100 | 91 | 38 |
| C_S2/right st3 | 铰链侧门框 | 100 | 97 | 95 | 45 | 0 |
| C_S2/right st4 | 把手侧门框 | 42 | 23 | 14 | 0 | 0 |
| C_S21/right st4 | 门板自由边 | 86 | 81 | 79 | 73 | 16 |
| 各 lane 各 stage | 把手 (depth) | 99–100 | 99–100 | 99–100 | 98–100 | 95–100 |

读法：**把手 depth 通道靠腕机就够（95–100%）**，与 base 相机布局无关。base 相机的全部价值在**门框 / 自由边 / base RGB 的把手冗余**。V0→V1a 的代价集中在 LEFT 门铰链侧门框（43→9）与 RIGHT 门 Stage4 把手侧门框（42→23）；V1a→V1b（把上仰角砍掉）的代价更大（99→39、94→59）。V3 在 Stage3–4 几乎丢掉整个环境通道。

### 2.3 腕机塔高（V4 / 决策日志 V28-D028，mesh 射线，COMPUTED）
`w4_tower_height_occl.py`，真实 link7/link8/gripper_base mesh，参考姿态，开度 o ∈ {0, 0.013, 0.020, 0.035}。遮挡者始终是 `arm_body6_to_gripper` + `arm_body8`——**降低塔高不解决上指遮挡**。把手条两端在 **depth** 的可见长度：

| 塔高 / θ | o=0 | o=0.013 | o=0.020 | o=0.035 | §3.1 近场门（≥20% @0.013 与 0.035） |
|---|---:|---:|---:|---:|---|
| 180 mm / 45° | 52% | 38% | 29% | 24% | PASS |
| 140 mm / 38.76° | 33% | 29% | 24% | 48% | PASS |
| 120 mm / 34.49° | 24% | **19%** | 19% | 43% | **FAIL**（19 < 20） |
| 100 mm / 29.13° | 24% | 19% | 38% | 38% | FAIL；且 RGB 内 bar 只剩 90% |

上指尖外侧面 depth 可见率在 180/140/120 mm 都是 13%（≥10% 门 PASS）。远场代价（v27 replay，C_S2/left st3，wrist 单相机）：把手 RGB 47→40→36%，门板自由边 depth 70→55→51%，门口地面 depth 9→2→1%。

### 2.4 哪些结论依赖旧轨迹
**依赖**：§2.2 全表；“第二台 base 相机在 LEFT 门 Stage3 值 +34 pp 铰链侧门框”；Stage3/4 的把手 RGB 份额。**不依赖**：§1 全部（几何 + 资产）；§2.1 canonical 结论（上仰角分水岭、head 位置为 0%、无人看得到门口地面）；§2.3 mesh 遮挡（静态参考姿态）。

---

## 3. 建议（Task 3）

| 布局 | Student 得到 / 失去 | 通过性代价 | 硬件复杂度 |
|---|---|---|---|
| **V0** 双 ±0.155 +15° + 腕机45°@180 | 现基线。多出 LEFT 门铰链侧门框（+34 pp）、RIGHT 门 Stage4 把手侧门框（+19 pp）、base RGB 把手的臂遮挡冗余 | **0 mm**（COMPUTED） | 2×D435i + 330 mm 横梁 + 2 外壳；trunk 半宽 0.140→0.200 |
| **V1a** 中置 [0.025,0,0.19] +15° + 腕机45°@180 | 保住把手侧门框（99–100%）、自由边（94–100%）、门口把手；失去铰链侧门框（LEFT 43→9）与一半冗余 | 0 mm；trunk 半宽回到 ≤0.165（可再缩到 0.140） | 1×D435i，横梁可删，线束减半 |
| V1b 中置 0° | 再失 60 pp 把手侧门框、35 pp 自由边 | 同上 | 同上 |
| **V2b** head 位 +15° + 腕机 | 抓握期门框/自由边 0%（canonical）；replay 上 LEFT 门 st3 门框 33%、st4 自由边 56% | 0 mm（位置更靠内） | 1 相机，但要在“头部”新增安装面 |
| **V3** 仅腕机 | 把手 depth 仍 95–100%；Stage3–4 门框 0–38%、自由边 9–70%；机器人在臂工作时对环境**失明** | 0 mm；trunk 回到 0.140 | 最简 |
| V4a 中置15° + 腕机38.76°@140 | 与 V1a 同的 base 通道；腕机远场 −7…−15 pp，近场仍 PASS | 0 mm | 塔架短 40 mm，力矩/共振更好 |
| V4b …@120 | 同上但**近场 depth 门 FAIL** | 0 mm | 最短塔 |

**排名：V1a ≈ V4a > V0 > V2b > V4b > V3 > V1b。**
理由：通过性不是判据（全部为 0）；那么第二台 base 相机唯一买到的是 LEFT 门铰链侧门框与冗余，而这两项目前**没有任何冻结验收判据**（plan §3.1 的前视/近场需求全部只约束腕机）。在硬件未设计、线束与标定成本真实存在的前提下，单台中置 +15° 是更好的默认；若 Owner 想保留双相机，理由应写成“Student 冗余/铰链侧门框”，不能再写成“通过性无代价所以无所谓”。V4a 与 V1a 并列，因为 140 mm 塔在近场门内、远场只掉个位数 pp，且减小塔架穿门板风险（planner_evidence `REPORT.md:90`）——但塔高变更会改变 `wrist_camera_tower` 碰撞体，属于 Wave A 前必须定死的项。

**若 Owner 选“单前视 + 腕机”，plan §3.1 的最小改动：**
1. §3.1 “base 相机 | 左右对称上仰 15°” → “**单台 base 相机**，trunk 系 `[0.025, 0, 0.19]`，上仰 **15°**（0° 不可接受：抓握期门框可见率 83%→7%，COMPUTED）”；新出 `U3_F45_C15.json`（删 `base_left`/`base_right`，加 `base_center`，wrist 块逐字不变）。
2. §1 决策表：D-06（左右对称 15°）标 SUPERSEDED，新增一条“单相机 + 15°”决策，重审触发 = 渲染验收显示 Student 需要铰链侧门框通道。
3. §3.2 G0-C2 判据“LEFT/RIGHT 相机导致的覆盖差 ≤ 5 pp”对单相机不适用；替换为“同一指标在 LEFT/RIGHT 门的差 ≤ 15 pp，铰链侧门框一项例外并单独报告”（现值 LEFT 9% / RIGHT 79–97%，会触发例外）。
4. §4.2 “base 相机支架与外壳并入 trunk 的 `<collision>`（8 个盒 + 2 个外壳盒）” → 4 个盒 + 1 个外壳盒；横梁 `0.022×0.33×0.008` 缩到 ≤0.09 长；trunk 侧向半宽从 0.200 回到 0.140。资产需重生成 URDF/USD 与 readback（HIGH_RISK，记决策日志）。
5. §8.5 / Student lane：`plan_id` 检查与 rig JSON 同步改为 `U3_F45_C15`。
6. §6.4 telemetry 的 `base_cam_ang_speed_p95_deg_s` 定义不变（刚性绑 trunk，与台数无关）。
**不需要改**：`clean_complete`、stage 判据、reward bundle、腕机 θ/塔高/近场需求、`wrist_camera_tower` 碰撞体。

### 三类结论的分界
- **(i) 几何已定**：相机总成对门口净宽的代价 = 0 mm；上仰 15° vs 0° 的门框可见率分水岭；旧 head 位在抓握期为 0%；所有布局都看不到门口地面；120 mm 塔架不满足 §3.1 近场 depth 门；降低塔高不改善上指遮挡。
- **(ii) 需要渲染 / Student 证据**：真实 RGB 与 stereo depth 的有效性（门板无纹理、MinZ 0.105、<0.2 m 精度）；门板/墙遮挡后的实际可见率；Student 能否只靠单视角定位把手与判断侧别；第二台相机的冗余是否转化为蒸馏成功率（= 现有 X-02）。
- **(iii) 只能等新 Teacher**：Stage3/4 的臂姿决定 base 相机的把手可见率与自遮挡；穿门偏航（现 0.22–0.35 rad）决定 p5 窄门是否仍有 23–28% 的机身-门框碰撞；单相机的盲侧是否被新 Teacher 的接近方向系统性命中。

---

## 4. 未运行 / 边界
未运行 Isaac、渲染、任何 v28 trace、硬件。§1 的腿部姿态取配置默认值（trace 不含腿关节），行走中的真实腿宽未知；§1 的碰撞判据把足球体当刚性障碍，实际是可迈过的落足点，故 W p5 的 23–28% 碰撞份额是**上界**。§2 不含门板/墙遮挡、不含 stereo 有效性；base 相机的臂遮挡只用 capsule 近似（与 precheck 同）。§2.3 的塔架支架包络仍是 180 mm 设计包络，故“自身支柱入画”一列对 140/120 mm 无效。未产生 durable memory candidate，未持有 writer/lease，未做 stage artifact handoff。

# Owner 请求与本次范围

2026-09-20 HKT。来源：当前 Codex planner 对话中 Owner 的原话；以下五项逐字保存。

> 接下来想基于v29 baseline开始N01的预研，帮我打包给pro模型让其提供建议/回答/完整方案：
> 1. 让其保持独立思考。 novelty/ 中的已有讨论仅做参考。
> 2. 以完整 v29 C002 为主底座，先保留 B05
> 3. 重新思考“恢复图” 设计，设计好哪些地方需要设计“恢复”环节？设计“退回哪里”
> 4. 重点思考：不能再把“Teacher 学会 → 常规蒸馏 → Student 自然继承”当作默认成立的链条。 teacher有环境创造失败场景，但是给student进行蒸馏时的teacher一般很难出现失败场景让student进行学习。
> 5. 那么一是训练有效的能够进行失抓等抗干扰能恢复的teacher，二是 teacher能力如何蒸馏给student？

本次是研究与方案交付，不是 N01 方法实现或实验启动。主底座是已接受的完整 C002，版本入口 `v29-c002-baseline`，保留 B05 七族把手。GPU1 的 `v29-handle-ablation-c001` 是另一组对照，不能作为本次主底座，也不预设它优于 C002。

当前 GPU0 baseline 与 GPU1 handle-ablation 的既有训练继续各自已登记的等待；本次不改变源码、参数、等待、资源或实验预算。审阅分支只用于交付资料，并非已创建 N01 实验工作区。

Owner 要求独立思考：既有“恢复图＋失效边界采样”是待重评的历史候选，不是必须证明的答案。可以缩小、重构或否定图的价值，但必须正面回答“何时恢复、恢复到什么物理条件、student如何学到并执行恢复”。不要用不存在的完美Teacher或最终相机/CAD作为无限延期的理由。

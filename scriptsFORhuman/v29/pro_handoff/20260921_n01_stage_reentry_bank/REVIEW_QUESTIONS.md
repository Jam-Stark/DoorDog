# 独立审阅问题与所需回答

先读OWNER_REQUEST和LOCAL_SOURCE_FACTS，沿SOURCE_INDEX查当前完整源码；形成初步结论后再读历史参考包中的本地建议与旧Pro回包。可以推翻当前建议，不需要为已有plan补论据。只提供结论、依据、取舍与可实施设计，不要求展示内部思考过程。

## 1. 恢复图的真实作用

说明N01应修改哪些Stage边/目标，而不仅是记录失抓类别。给最小充分的转移表：触发条件、目标Stage/子目标、实际策略控制方式、完成后的重入条件。明确哪些callback只属于首次前进，哪些在重入仍要执行；时间、预算高水位、重复收入、累计arm target和RNN如何连续。

Teacher可以看到特权训练目标；Student只有既定81D＋RGB。说明恢复图怎样参与训练和评价，以及部署时谁决定动作，不能把真值Stage/contact变成替Student操纵夹爪或移动手臂的隐藏控制器。

## 2. 门状态与重入是否相容

不要凭Stage3的名称断言它一定要求重新下压。当前源码的3→4门角/握持条件、近关闭解锁reward与Stage4 reward condition必须一起考虑。

请具体推演：

- 门已经明显半开，新握持成立；
- 门已解锁/门舌已离开门框，但角度尚未达到当前Stage4资格；
- 恢复期间门回关，甚至重新上锁；
- 把手已脱离，但当前已满足正常通过条件，不再需要扶门。

为每种情况写出目标、进入/退出条件与reward配合。可使用当前0.1/0.25 rad作解释性反例，也可以提出更合理的物理判据，但不能把未经本地运行的推演写成实验结果。

## 3. 主动恢复场景与Student复用

先判断是否值得在首轮主动加入bank，再给最小实现；不默认必须做，也不默认旧v27方案已经有效。

说明复用单位是场景规格、物理snapshot、轨迹还是它们的组合；需要哪些门/把手/机器人/控制状态，怎样处理不同env origin、几何、joint ordering和两模型观察差异。比较从困难状态开始新episode与恢复中途历史两种语义，说明何时可以从各自hidden初始值开始，何时确实需要prefix重建。不要自动要求每个场景完整replay，也不要把Teacher hidden复制给Student。

给出Teacher/Student数据流：谁执行动作、何时采集真实RGB、Teacher怎样在当前状态及历史上提供12D标签、怎样确定其后缀有用、Student怎样亲自经历并完成恢复。旧v27 bank的计数readout不当作可重放数据集；本次v28 force日志也不是完整机器人snapshot。

加入bank会改变当前natural-reset和数据来源合同。请给最少必要的对照设计，明确是单独测外力作用，还是测外力＋场景采样的整体方法；不要为所有可能性同时增加多组训练。若用旧Teacher预填bank，明确它改变了什么研究假设，不把权重scratch与没有旧专家数据混为一谈。

## 4. 方案取舍与交付

效果优先，简单高效是Owner偏好。请指出哪些复杂度确有用途、哪些可以删除；不要预设动作primitive架构。必要时查一手论文/官方作者代码，引用确实支持相关结论的来源；不能用文献替代本项目的物理和控制条件。

对话中先直接回答三问、给推荐主线和关键取舍。另附普通`pro_delivery__full_review.zip`（压缩后≤95MiB），至少包含：

- `FULL_REVIEW.md`：详细回答、证据、替代方案和待本地证明的点。
- `LOCAL_WORKER_PARSE_PROMPT.md`：与本包同名文件完全一致，并在对话中显示可复制版本。
- 可附`STAGE_REENTRY_AND_BANK_SPEC.md`：转移表、场景/状态字段、数据流和少量伪代码。

把ZIP附在Pro对话，由Owner下载再传回本地；不上传Pro答案到Drive。没有附件能力就明确`NOT_ATTACHED`，不虚构链接。云端审阅不声称执行了Isaac/GPU仿真，也不要求本地先建设一整套回归/护栏测试。
